# 兰台前端 · 设计规格书（v1 · 落地前置）

> 本文是 `holo-paper.html` 原型锁定的设计决策，供 `src-ui` 工程落地对照。
> **「已锁定」是契约，落地时不许回退；未列出的像素细节（字号/间距/图标形状/hover 态）可在真环境再磨。**

---

## 0. 一句话定位

**兰台＝汉代皇家档案典籍库。** 产品不再是「聊天窗」，而是**一部正在被编纂的案卷**——人在纸边批注、AI 居中撰文、机器贴底注记。核心是把「等权消息流」换成「注疏层级」。

---

## 1. 版式范式（已锁定）

**注疏横排**。文字全程横排（代码、英文、URL 不折行），但骨架是古籍注疏的层级，不是聊天气泡：

- **正文**（答 / 策）居中，是案卷的「经文」，正文位、大一号（17px 起，行距 2.0）；
- **来文**（问）是人的批注，手迹位 + 朱砂深 16px/1.9（B4 环1 定稿），左侧 2px 红批线；
- **夹注**（思）缩进列边（约 86% 宽），石墨铅笔，小一号，虚线勾边；
- **脚注**（器）贴底小字，上方一道 44px 石青注线引出；
- **抄录**（码）栏内图版（3px 硬左线 + 上下规线 + 米黄底）；
- **卷首**：**版心天头**（**2026-09-16 用户拍板 B 案重排**）——玉徽（亭台图标 `#icon-lantai` 26px）· 机读眉行（卷次「案卷 Nº 12」）· 题字（36px）· 机读档行（**立卷日「二〇二六年八月廿二日」· N 块**）四行**同轴居中于 720 版心**，底部硬规线 + 左缘朱砂短横（**2026-09-02 改档只挂活跃卷**——整屏至多一处红（浸墨③），红在哪卷即活卷；对原型 `.folio-head` 的主动偏离——原型先于真纸材质批与一纸多卷定案）；规线与版口钮同样只画版心宽。卷号在卷首**恰出现一次**（有名卷在眉行、无名卷在题字 fallback，眉行退为「兰台 · 案卷」文类行）；**卷名真源 = 卷快照 `label`（空 = 未命名）**——「有名/无名」判据、首条来文派生、显示兜底「案卷 N」（一律取**档号**）的单一真源 = `src-ui/src/state/volume-name.ts`（2026-09-18 命名收口：起卷不写数字名，日志头行不再带卷名）；立卷日真源 = 卷日志头行 `createdAt`，旧卷无此字段 = 档行只显块数（不编造）。**重排前的病灶**（实测）：玉徽居中于整张纸（默认 1440 宽流区的中轴）、眉行/题字/档行却左齐于纸缘内距 16px，而正文块居中于 720 版心——题字比正文左缘还左 344px。原型的「钤印居中于天头」手势保留，改居中于版心；
- **界栏**：正文列左右各 1px 栏线（`--rule-strong`），页边注在左栏外（**2026-09-02 翻案**：08-30 把界栏落成「流区四边版框双线框」的读法判死——纸缘交还材料、改物理包边（见 §10 纸层次·流区真纸）；版框＝「印在纸上的墨」，流区这张「叶」不印框——WebGL 探针 `prototype/webgl-paper.html` 判读定案）；
- **划词**（2026-09-02 视觉迭代）：流区选区＝手写朱笔下划线（`src/paper/sel-ink.ts` 纯函数——主笔朱砂实线 1.7px＋淡墨 echo 第二笔，种子取选区文本 hash、同选区恒同线；跨块选区也画线）；原生 ::selection 洗底在流区退役（输入面照旧）——纸不动、只落墨，被人手划过的字落朱线；
- **选区政策**（**2026-09-17 全局化，用户报「太多不该触发文字选中的地方」触发**）：**全应用默认不可选，正文自证可选**。`foundation.css` 在 `html` 上落 `user-select: none`——**界面是界面，不是可复制的文档**；放开面只有两处：① **输入面**（`input / textarea / select / [contenteditable]` 一条，Chromium 下祖先 none 会连坐表单件的拖选）；② **正文容器自持** `user-select: text`（纸壳 `.pp-block` 即范式，块内结构件/交互件再各自撤回 none）。**判定**：新增产品面若确有可复制正文照 ② 自行放开，界面件一律不写。**旧账的病灶**：此前没人管这件事，谁出事谁补一条（坞拖动期 / 纸条 / 小地图 / 书脊行 / 插件窗题条各写一次）——补一处漏一处，拖动扫选、双击选词、Ctrl+A 把整壳收进选区在其余面上照旧发生；而纸上挂着 document 级 `selectionchange`（选中浮钮 + 划词朱线），任何一次扫选都可能唤醒画布上的划词动作；
- **划词的第二道守卫**（2026-09-17）：`user-select: none` 只挡「从该面起选」，挡不住「按住家具再扫进纸面」——浏览器把选区锚点**夹到最近的可选内容**（块内文字），于是家具起手的拖拽在锚点上与真划词无法区分，浮钮照弹（同族实测见 paper-minimap 的 `preventDefault` 注：拖小地图会选中画布文字）。故两个选区消费面（浮钮 / 划词朱线）共用**起手面判据**：按下那一刻记下是否落在 `.pp-block` 内，本次按住期间的选区按起手面算；块内输入件（准奏卡意见框）另由 `isEditableSurface` 挡（在自己输入框里选一段不该弹抽纸条浮钮）。**代价（明账）**：纯键盘造出的选区沿用「上一次按下的面」——先在块里点过即照常认，冷启动直接 Ctrl+A 不认（全选不是划词）；
- **印章**：朱砂「蘭臺」二字竖排印，作品牌签名。

---

## 2. 字体系统（2026-09-10 三体换代拍板重锁）

| 角色 | 值层 | 装载 | 备注 |
|---|---|---|---|
| 正文位 `--f-song`（原宋体） | MiSans | `src/app/fonts.css` @font-face（`src/assets/fonts/MiSans-VF.ttf`，VF 单文件 100-900 全字重） | 原宋体/衬线正文 |
| 手迹位 `--f-kai`（原楷书） | MiSans | 同上 | 只用于「人的」来文——文类语义保留，手写感随三体退役 |
| 机读位 `--f-mono`（原等宽） | MiSans | 同上 | 代码/工具/编号——非等宽对齐为拍板接受的代价 |

字体栈（CSS 直抄，文类语义键保留、值层统一）：

```css
--f-song: "MiSans", "PingFang SC", "Microsoft YaHei", sans-serif;
--f-kai:  "MiSans", "PingFang SC", "Microsoft YaHei", sans-serif;
--f-mono: "MiSans", "PingFang SC", "Microsoft YaHei", sans-serif;
```

**装载**：`src/app/fonts.ts` 只 `import './fonts.css'`（本地 @font-face，打包进应用不依赖系统安装）；fontsource 四包（eb-garamond / ma-shan-zheng / ibm-plex-mono / noto-serif-sc）已卸载。

> 关键纪律：**斜体走合成伪斜**（MiSans 无 italic 字形，`font-style: italic` 由渲染器合成——拍板接受）；canvas 侧字面量栈（type-tokens `FONT_STACKS`、InkLayer `LABEL_FONT_STACK`）与 tokens.css 同步改。

---

## 3. 墨色系统（已锁定 · 矿物颜料）

| token | 值 | 语义 | 用在哪 |
|---|---|---|---|
| `--ink-1` | `#26221C` | 松烟墨（正文） | 答 / 策 / 码正文 |
| `--ink-2` | `#55503F` | 次级墨 | 规线 / 次级文字 |
| `--ink-3` | `#8A8172` | 三级墨 | 注记 / 页码 |
| `--ink-4` | `#B5AC9A` | 弱规线 | 分隔 |
| `--seal` | `#A63A2E` | 朱砂（人的批改） | 印章 / 来文 / 圈点 / 朱批 |
| `--seal-deep` | `#8C2F26` | 朱砂深 | 来文文字 |
| `--indigo` | `#3A5B7A` | 石青（机器/仪表） | 脚注 / 拟策策面（左线·淡底·文类签）/ diff 新增 |
| `--graphite` | `#6F6E68` | 石墨（铅笔灰） | 夹注 / 擦改 / diff 删除 |
| `--paper` | `#F6F1E7` | 纸面 | 画布 / 页面底 |
| `--paper-deep` | `#EFE8DA` | 深纸 | 输入条 / 图版底 |
| `--paper-margin` | `#EDE5D5` | 页边 | 舞台底色 |

语义状态（保留原 `--pass/--warn/--fail` 语义，降饱和到矿物色）：`--pass #5E7A55`、`--warn #B0782E`、`--fail #A9443F`。

**一条铁律：朱砂 = 人，石青 = 机，石墨 = 夹注，墨 = 正文。** 不许换义。
> 2026-09-16 口径订正：原写「石墨 = 草稿」与值层不符——`--graphite (#6F6E68)` 实挂夹注 / 擦改 / diff 删除。
> **「草稿」语义由虚线（`--rule-dashed`）表达，不占颜色位。**
唯一豁免（2026-08-23 B5 环2 用户拍板）：**抄录（diff）加删色走红绿墨色化**——add=松绿（`--pass`），del=朱砂深+删除线。del 的朱砂深不表「人的声音」，读作擦改/勘误红笔；加删红绿对是代码块的视觉直觉，纯红纯绿不入纸面，必须落在矿物色族。

---

## 4. 块类型映射（已锁定）

现有 `paper/block-model.ts` 的 `BlockKind` 无需改动，只改渲染层视觉：

| BlockKind | 文类签 | 字体 | 墨色 | 视觉 |
|---|---|---|---|---|
| `user` | 来文 | `--f-kai` | `--seal-deep` | 左 2px 红批线 + 圈点关键词；16px / 行距 1.9（B4 环1 拍板：字号收到正文 17 之下，朱砂深保持——问仍是唯一彩色声音）；附图缩略行（2026-09-09 multimodal-image B4）：64px 方界 tile 规线细框 + wrap 居中多行 + 点击放大浮层，几何 token `--pp-ch-userImages-*`（measure 镜像 `userImagesRowHeight` wrap 行数整除推得）；未命中预览/读失败降级 mono 占位盒 |
| `markdown` | 正文 | `--f-song` | `--ink-1` | 17px / 行距 2.0，居中主角 |
| `reasoning` | 夹注 | `--f-song`（英文走 Garamond） | `--graphite` | 86% 宽、虚线勾边、擦改痕迹 |
| `diff` | 抄录 | `--f-mono` | `--ink-1` | 3px 硬左线 + 米黄底图版，add 松绿（--pass）/ del 朱砂深+删除线（B5 环2 红绿墨色化定稿——铁律在 diff 语境豁免：删行读作擦改非人语） |
| `tool` | 脚注 | `--f-mono` | `--indigo` | 44px 注线 + 11–11.5px 小字 |
| `plan` | 拟策 | `--f-song` | `--ink-1` | 石青策面（2026-09-10 拟策卡渲染专项，用户拍板）：顶硬线 + 底软线保留 + 3px 石青硬左线 + 石青淡底（indigo 4% 兑纸面）+ 横向内距 16——与来文的朱砂左线对仗（人声朱 / 机策青）；内容走完整 markdown 体（`.pp-pc-body` + `.pp-md` 族共享，正文位 17px/2.0），行号机制（01/02/… 石青序号）退役；方案区 + 钤印三钮不变 |
| `notice` | 贴黄 | `--f-song` 小字 | `--warn` | 系统通知（建议作「贴黄」纸条） |

> `notice`（系统通知）原型未覆盖，建议按「贴黄」（古代奏章上贴的黄纸条）处理：小字、土黄底、一条弱规线，与正文明确区分。

### 4.1 正文（markdown）子元素版式与折叠机制（2026-08-30 补记）

正文块（`markdown`）不再是纯文本平铺——子元素按注疏版式降级渲染；结构模型 = `paper/markdown.ts` 单一解析（渲染与测量共用，块高镜像 `measure.ts` 的 `MD_*` 常量，CSS 镜像 `.pp-md-*`，改版式三处同步）：

| 子元素 | 版式 |
|---|---|
| 段落 | 17px/2.0，段间 14px（旧 10px 段距小于行距、段落黏连，专项提到 14） |
| 标题 h1–h4 | 20/18/16.5/15.5px 正文位加粗，padding 承担上下距（首元素 margin 逃逸会破坏测高，禁 margin-top） |
| 列表 | 标记列等宽小字绝对定位（ul `·` / ol `n.`），条目缩进 26、嵌套再缩进 22 |
| 引用 | 石墨左线 + 缩进 + ink-2 |
| 图码 | 石青左线 + 深纸底（独立 `diff` 块之外幸存围栏的兜底面） |
| 远端图（2026-09-09 multimodal-image B4） | 独立行 `![alt](http/https)` 固定盒 160px（`--pp-md-imgBoxH`，chem 先例同族——加载/失败态都不改版面，measure 静态镜像即精确）；协议白名单过在解析层（非 http/https 降级 alt 文本，空 alt 整行不产块——data: 巨串不灌纸面）；周框 `--rule-soft` + 深纸底；加载失败换 mono 弱墨 alt 行（盒界常在，错误不静默） |
| 表格 | 等宽 11.5px + 弱规线（`.pp-grid-table` 同族） |
| 行内 | 加粗 600 / 斜体 / 删除线石墨 / 行内码等宽小号 chip / 链接石青下划线 |

**块体白空格纪律**：`.pp-body` 恒 `white-space: pre-wrap`——measure.ts 的 canvas 测量一律按 pre-wrap 计行，渲染端折叠换行会产生「测得多、渲染少」的幻影块间距（2026-08-30 间距根因之一，已修）。

**折叠机制**（`paper/fold.ts`；规则态 + 用户覆盖态）：夹注（reasoning）恒折叠——流内只留一行预览；脚注（tool）/程文（code）按状态折叠——`running`/`error` 展开（过程与错误可见），`done`/`pending` 收起（完成即收）；折叠行 = 块顶等宽小字（单工具 `▸ 名字 判别摘要 · 输出 N 字` / `▾ 收起`），用户点击写入覆盖态（状态翻转自动收回不打架）。**判别量**（stream-rhythm 刀4a，2026-09-03）：压缩的是重复不是信息——工具组折叠行露判别字段（`▸ 工具 ×3 · read_file src/a.ts / src/b.ts / src/c.ts`；shell 露命令；截断/同值去重补 ×N 总账；无判别字段回退名字计数），折叠行恒一行（10px mono，测高镜像零变化）。折叠态计高与渲染同步镜像：脚注折叠全收、程文折叠收程序体留输出/错误（执行结果一眼可见）。

**文类签（页边注）**：来文 / 正文 / 夹注 / 脚注 / 抄录 / 拟策（贴黄），配机读码 `USER / AGENT / THINK / TOOL / CODE / PLAN`。

### 4.2 会话流节奏（2026-09-03 stream-rhythm 立法 · D1 试值待真机终审）

会话流不是等距块栈：事件按语义关系组成**工作单元**（tool / toolgroup / code / subagent 连续运行 + 挂靠夹注；error 块起「恢复单元」），单元内紧、单元间松、阶段完成换气。规则真源 = `paper/group.ts`（单元封套）+ `paper/canvas-math.ts`（间距档 ANCHOR 表），钉值 `tests/paper-stream-rhythm.test.ts`。

| 档 | 值 | 语义 |
|---|---|---|
| 单元内 intra | 32 | 行为链紧密纵向连接（同一工作单元相邻块） |
| 块距（基线） | 48 | 无节奏信息的外部调用面兜底（B1 不动） |
| 单元间 unit | 64 | 同回合内工作单元切换 |
| 转折 recovery | 96 | Error 起的恢复单元 / 墓碑前置放空——打破正常节奏 |
| 阶段 stage | 96 + 弱线 | 来文开新阶段：留白 + 细线（`--rule-soft`，落间距中线） |

叠加律：上方是来文恒尾距 8（B1 反转：asterism 让位优先于节奏档）；B1 来文间距反转与阶段档共存。

两宪法（单元封套 pass 的结构保证）：**封口纪律**——已封口单元不可变（id 锚首成员、成员只增不减），活尾（streaming 消息）才许重排；**跨消息前瞻禁止**——挂靠（夹注吸入下一工作单元）只在同消息内发生，单元不跨消息（消息边界即收口）。阶段标题不自动生成（自动起名会撒谎）——plan 标题 / commit message 等显式信号由后续批次继承。

**刀3 布局保证（2026-09-03 补记）**：封口前缀 = **刚体**——追加事件只许整体平移，封口块的节奏档/间距/相对位置逐字段不变（布局级测试钉死，滚动锚定的数学面）；流式重排平滑 = `pp-stream-live` 下 transform 过渡 200ms（旧块被轻轻推开，绝不瞬移、绝不挂 left/top/height）。**组原子性**：折叠组 = 布局栈单条目（子卡摘除——虚拟化窗口对组只能整取整舍），展开组成员在栈内连续（窗口切不出交错序）；长流（千块级）全链回放有性能烟测预算。**目次带阶段导航（刀4）**：阶段锚消费工作单元（`buildStageAnchors`，阶段 = user 单元，与阶段间距/细线同真源），hover 兜底带阶段序（`阶段 N · 首句`）。

**Error 墨色家族（刀4c 确认，现状合规零改动）**：错误状态色 = `--fail`（#a9443f 墨浓红，非纯红非朱砂——B5 红绿墨色化延伸）；错因墓碑（turn-error）= 贴黄款——朱砂淡底只作纸晕（`--seal` 7% 混纸），墨身走 `--ink-2`；工具/程文错误输出与状态签走 `--fail` 单一真源。**朱砂 = 人铁律不挪用**：错误文字永不直用 `--seal`。钉值 `tests/paper-visual-decisions.test.ts`（Error 墨色家族三断言）。

**刀5 族节奏批（2026-09-03 补记——用户真机判「整个会话流还是瀑布」的根治批）**：节奏词汇的判据从块 kind 升级为**节律族**（读/写/验证/落款——`grammar.rhythmFamilyOfBlock`；other 不表态不断节奏，远端/未知名不破语法铁律的节奏层延伸）：**族边界切单元**——已知族切换（读→写、写→验证、验证→落款）即封口旧单元开新工作单元（errorish 转折优先于族界；recovery 同族重试续收、异族切出），WorkUnit 带 family；**折叠组按族切分**——连续工具族变即断组（「读 ×3」「写 ×2」「验 ×1」各自成行，判别量各露各的），unit 64 在族间开火、32 收在族内；**单元界短规线**——work 单元首成员上方 96px 短线（比阶段全宽线弱一档，落 unitGap 64 中线 top -32；叙述不碎、转折自宣告、来文后 B1 反转区无线位）——线语法三级：全宽 = 阶段界 / 短线 = 单元界 / 无线 = 同单元收紧；**验证链毕锚**——verify 工作单元链毕出「✓ 阶段完成」小字（ink-3 mono，链末块下方，不占块高），失败转折（后继 recovery）不发、流式在跑不发——✓ 不说谎；**组头族签**——折叠行前缀族字（`▸ 读 ×3 · read_file a.ts / b.ts / c.ts`，验/落同款，混族回退「工具」）。钉值 `tests/paper-stream-rhythm.test.ts`（族切组/族切单元/other 不表态/recovery 族续/链毕四态/单元线规则）+ `tests/paper-visual-decisions.test.ts`（刀5 四断言）。

**2026-09-06 尸检补记（用户真机报「渲染隐约不对、说不出病灶」）**：线语法两条隐蔽执行修正——①**单元线实现机制**：块级 ::before 与脚注族注线（`.pp-tool/.pp-toolgroup/.pp-code::before` 石青身份标记）同槽同特异性，源序落败被静默顶掉 → 单元线在工具族首块上整体消失；改实元素 `.pp-unit-rule`（壳层条件渲染，钉值不变），线语法三级语义不变；②**栈首无线位**：阶段线/单元线的负偏移对栈首块画进卷首头带（-48 落 head 底规线上方 ~18px 每卷必现错位线）——线标记「上方有界」，栈首之上只有卷首（天然界），`rhythmAssign` 以 `i > 0` 守卫两线发放（节奏档不动）。回归钉值：paper-visual-decisions「槽位独立性」+ paper-stream-rhythm「栈首不发线」。

---

## 5. 术语表（已锁定）

| 旧 | 新 |
|---|---|
| 会话 | 案卷 |
| 新建会话 | 新建案卷 |
| 发送 / 向 Agent 写字 | 拟文 |
| 星图 / 依赖图 | （退役，不再作为用户可见卖点） |
| 工作台 | 兰台 |
| 信号源 | 提供方（2026-08-24 补录：观测站时代旧词退役，对齐 DeepSeek Harness 官方中文定译） |

---

## 6. 交互（2026-09-05 起含手势修订）

无限画布 + 钉住 / 收回 + 小地图 + 抽纸条，已在 `PaperPanel.tsx` 与 `paper/` 内核实现。**2026-09-05 pin-strip-rework 批修订**（`docs/plans/pin-strip-rework-plan.md`）：拖块改**松手定夺**（拖动全程纯预览，带外松手落钉 / 带内取消回槽；眉批撕出保留首动建钉——instant 族例外）+ **拖选跨带成条退役**（拖选只做选择；抽纸条唯二入口 = 按住已有选区拖出 + 选中浮钮）+ **带显形**（拖拽中来源流区提落影 `--shadow-sheet-lift`——400px 窄带从暗判变可见边界，落点悬带内块影转「松手回流」态）+ 纸条销毁两击确认 + 可发现性一次性眉批（localStorage 旗标）。钉住手势本体（按住文类签拖出 = 钉、洞点击 / 悬停带内松手 = 收回）与已修过的手感（抓取偏移按实际渲染位反推、防跳变）不变。

**2026-09-18 出处引导批新增**（`docs/plans/pin-provenance-plan.md`）：钉住块的**来路**成为一等件——病灶是「洞」这条唯一线索是**视口内**的（流自锚点向上生长 ⇒ 洞越漂越远；渲染窗口化 ⇒ 洞离屏时连 DOM 都没有）。三件：

| 件 | 形态 | 判据 |
|---|---|---|
| **出处行** `.pp-prov` | 页边注（文类签）第三行「摘自 卷名 · 状态字」，**常显** | 不依赖任何视口内目标；卷名权威链 = 活卷名 → 建钉冻结的 `pin.source.label` → 档号兜底；超长卷名纯层截断 10 字 |
| **引线** `.pp-tether` | hover 钉块 → 一丝**手绘朱笔**（**划词朱线同族**：微伏 + 定种子 + 手绘平滑 + **恒定墨宽**）从钉缘引到洞缘；起笔留白 6px、收笔朱点；**洞离屏时线照样出屏** | 一屏一线；选边净空优先（相叠取左缘，不横穿钉身）；层 = **屏幕坐标**（同 `.pp-sel-ink`，墨宽不随缩放变）；z 4（纸与块之上、坞与浮件之下） |
| **溯源** | 点出处行 → 飞到源洞（落洞中线）+ 洞点名一拍（1.6s 自散） | 四态：活钉 / 不在卷内 / 未摊开（点 = expand 摊开并定位）/ **已删（不可点）** |

墨阶：出处行 `--ink-3`（常显注记档）；引线与洞点名 `--seal`（朱砂 = 人；hover 瞬时态不占「每屏至多一处红」常驻名额，与文类签 hover 同族）。引线的笔墨语法见 `paper/sel-ink.ts` 判例（**手感来自微伏不来自粗细变化**——`PaperPanel.css` `.pp-tether` 头注与 plan §1.1 记了这次转录与三处弃案）。

**2026-09-19 便条批（用户拍板「甲+丙」）**：钉住块与纸条的**纸面语言**整体换装——手势与语义零改动，只有三层：

| 病灶 | 治法 | 落点 |
|---|---|---|
| 墨占 100%（流内 720 墨借 1440 纸的左右各 360 白边，钉到桌面上后那份白边不存在了，只剩 4px 裱边带顶着字） | **纸内白边**（钉块 34/44/38，纸条内距 10/12 → 18/20；墨占 100%→88%、95%→92%） | `CHROME_TOKENS.pin` / `.strip`（真源）→ `--pp-ch-pin-*` / `--pp-ch-strip-*`；钉块几何高的镜像 = `measure.ts` 的钉住态分支（白边 + 报头 + 按收窄测宽） |
| 常驻件全挂在纸外（文类签 + 出处行 `-128px`、竖排「钉住」签 `+36px`、收回钮 `-30px`） | **报头收进纸内**（方墨点 + 文类 + 序号 ……… 出处行，弱规线一行）；竖排「钉住」签**退役**——身份改常显方墨点（钉住是常驻状态，不该靠 hover 才现身）；收回钮留在纸外（hover 瞬时件，不占构图） | `.pp-block.pp-pinned .pp-kind`；`.pp-pin-hint` 与 JSX 一并删除 |
| 材料只有剂量差（钉块 = 流区同纹理 + 同四层配方缩水版，与桌面只差 11 阶色 ⇒ 只能靠影子读；纸条连纹理都没有） | **三分材质 + 贴平**：桌面（有纹）/ 流区（有纹的纸）/ 便条（`--paper-deep` 实色无纹）；纸内件**无投影**（§11 原表列，09-05 批曾越）；「拿起」的影只出现在拖拽携带态（压印族 `--elev-raise`） | `.pp-block.pp-pinned` / `.pp-strip` / `.pp-block.pp-dragging` |

同批：纸条题签由常显朱砂回落墨阶（`--ink-3` + 方墨点——每多一张纸条就多一处常驻红，与 §10 法则 3 相抵；朱砂留给两击销毁确认态）；纸条补**来源行**（`.pp-strip-src`「摘自 卷名」，取抽取那一刻的 `source.label` **快照**——拷贝语义：源卷改名不追改，与块级钉的活卷名出处行是两套语义；旧存档无此字段则整行不渲染）。判读件 `prototype/pin-strip-ab.html`（真 CSS/真资源 A/B 台）。

**2026-09-20 动作行造型批（用户拍板「丙」，病灶 = 「这些按钮真的太丑了」）**：块 hover 出来的动作行从「四枚 hairline 方框」改隶**注记族**——诊断五条 / 四候选出图出数 / 明说的代价见 `prototype/msg-ops-style-ab.NOTES.md`（台架 `.html` + 走查脚本可复跑），纪律守护在 `tests/paper-msg-ops-hover.test.ts`。

| 病灶 | 治法 | 落点 |
|---|---|---|
| **方框用错了语法**：`border + paper 底` 是全产品**常驻件**的语法（文类签 `.pp-kind` / 眉批钉 `.pp-marginalia-pin`）；hover 瞬时件一律去框纯文字 + hover 朱砂（`.pp-fold` / `.pp-marginalia-toggle` / `.pp-tool-btn`——后者 CSS 里留着「盒→线」的先例注）。四枚方框随 hover 一起长出来 ⇒ 与「一方向至多两层框」的版框层级打架 | **去框素面行**：11px 纯文字 + `·` 分隔（落在 gap 正中）+ hover 一件转朱砂并带一丝朱砂底线；命中盒靠**隐形 padding** 撑到 26×24（旧写法 19px 已顶格，要加高只能把可见的框画大）。行盒自持纸色底 = 隐形遮罩（行越出块底时盖住被越过的字） | `.pp-msg-ops` / `.pp-msg-ops button` / `button + button::before` |
| **来文块的行是孤儿**：题签 / 正文 / 花押全在版心轴上，只有动作行 `left: 0`（实测行中心比块中心偏左 281px） | **来文块的行居中**（`left: 50%` + `translateX(-50%)`）；回复块保持左齐（与左齐正文同轴） | `.pp-block.pp-user .pp-msg-ops` |
| **一个动作两个入口**：行内「立枝」按钮 + 块右端独立「枝」方框——两种字形、两处落位、右端一枚孤框 | **握把并入行尾**成六点握把（`::before` 画的 2×3 点，字形无关、不吃字体回退）；块外落位与 2px 缝桥面两段**删除**（行盒之内没有缝）；键盘路径照旧 = 行内「立枝」按钮 | `.pp-branch-grip`；`.pp-block:not(.pp-user) .pp-branch-grip::before` 与 `.pp-block.pp-user .pp-branch-grip` 整段删 |
| **等级不分 + 节奏不齐**：改写/重发（撤轮，会丢东西）与抄录（无损）视觉全等；单字双字混排（29.2 / 40.4px） | 标签改**文言双字**（改写 / 重发 / 抄录 / 立枝）⇒ 四件等宽 37.3px；置灰降级由 JSX 内联 `style{opacity}` 收归 `:disabled` | `use-block-ops.ts`；`.pp-msg-ops button:disabled` |

代价（明说，不藏）：回复块那条悬空腿 19 → 24px（多出的 5px 是透明 padding，不显形，但命中区会盖住下一块顶部 5px）；握把不再靠「字」表意（语义落 `title`）；11px 小字的可读性押在 `--ink-3` 与纸色的对比上（与 `.pp-fold` 同档，真机偏淡就抬一档到 `--ink-2`）。

---

## 7. 锁 vs 放

**锁（契约，落地不许回退）**：注疏范式、字体栈、墨色语义、块类型映射、术语、钉住手势、界栏/印章/亭台图标。

**放（真环境再磨）**：精确字号与行距、图标形状、hover/focus 态、空态 / 加载 / 流式 / 错误态、擦改痕迹浓淡、界栏粗细、notice「贴黄」的具体样式。

---

## 8. 落地清单（前置工作）

- [ ] `src/app/tokens.css` 换成 `lantai-tokens.css`（`--obs-*` 退役或并行过渡）
- [ ] `src/app/fonts.ts` 换字体（见 §2）
- [ ] `src/app/panels/PaperPanel.css` 按 §4 重排七类块（灰框 → 注疏）
- [ ] 块体渲染器（`composition/renderer-service` 的 `resolveRenderer`）按文类换墨色/字体
- [ ] 新屏：案卷首页（SessionsHome）与设置（SettingsPanel）换兰台皮
- [ ] 亭台图标 `#icon-lantai` 落成 favicon / 应用图标
- [ ] 文案全局替换（§5 术语表）

---

*原型文件：`holo-paper.html`；token 文件：`lantai-tokens.css`。*

---

## 9. 创作坞 v2（2026-08-31 落地）

创作坞（composer dock）从「贴底输入条」升格为「案头」——纸墨身份不动，交互模型对齐书房隐喻。八件定案：

1. **度量纪律**：坞本体锁 880px 版心居中（`.pp-composer max-width`）；超宽屏全宽输入行退役。纸色坞底与顶边规线仍全幅。
2. **两态同位**（2026-09-02 拍板 C）：创作坞恒居中抬起 `var(--composer-rise)`（96px 固定——不再随窗口高度 12vh 浮动，不同窗口大小坞位恒同）。零摊开卷的**案头态**只再管形态：退匣直书（`.pp-composer-slot.pp-at-desk`——形态类刻意不叫 `pp-desk`，避免与世界层桌垫同名撞车）、空态题字上移 36% 让位、坞下方**出流悬挂**签条架（最近三卷「续 · 卷名」签条，手迹位、hover 朱砂，点击 `space.expand` 摊开——架出没不推坞位）。落笔发出首句（开口即开卷）后换装常驻匣（坞顶硬线 + 版口钮），位置不动。**（2026-09-17 浮动化：96px 坐底抬高 = 出厂默认位与吸附锚之一，坞可由用户拖离底带——见 §9.2。）**
3. **附着面**：`引`（工作区文件模糊引用：`list_directory` 递归摊平 + 子序列匹配，basename 加权）→ 夹签入卷；**拖文件入卷**（Tauri `onDragDropEvent` 原生通道——HTML5 drop 在 T2 WebView 永不触发，C10 尸检课的偿还），悬停坞体亮**界栏**（朱砂细线 + 纸色加深 +「松手入卷」批注），松手命中才入卷；`夹`（file picker）保留为兜底。贴图入卷**刻意不做**——agent 无读图链路，贴图路径是空头支票（C10 同款判断）。
4. **渐进披露**：占位符只留一句（`拟文…` / `落笔即另起一卷…`）；键位收进**律**册（设置行右端浮层，13 行键位总览）；历史导航提示改**一次性眉批**（有历史且首聚焦时浮现 6s 自散，localStorage 旗标）；流区拖拽语义以原生 title 就地提示。
5. **墨量线**：坞底 1px 墨线 = 本卷上下文占用占模型窗口比例（2026-09-07 分母接线：per-model 覆盖 ?? 目录声明，`modelContextWindow` 与运行时压缩阈值同链，网关命名空间 id 目录不中时由设置页覆盖兜底），近满（>80%）转朱砂；裸 `[0]` 徽标退役，读数进 hover。窗口未知（0）或零占用不显（不编造）。**2026-09-13 分子换轨**：旧口径把「最后一次请求的 `total_tokens`」当「本卷已用 token」（既含输出、又只是单次请求量，与「上下文占用」不是一回事）；现改读 token-meter 的**投影占用**（提供方 prompt 侧压力 + 采样后载荷增量），无回报时回落载荷估算并在 hover 标注「估算」——同一条线现在报告的就是压缩判定看的那把尺子。
6. **钤印拟文**：发送钮墨色实心方章（`.pp-send`），hover 翻朱砂——人=朱砂语义：发送是用户的动作。
    **钤印单钮三态（2026-09-03）**：两枚印同构（实心 + 硬偏移投影 `--shadow-anchor`），色相即语义——墨印=拟文 / 朱印=停。单一按钮位随状态翻转：
    - 空闲（任意输入）→ **拟文**（墨印）：发送新轮；
    - 运行中 + 输入框空 → **停**（朱印 `.pp-stop`）：中止回合；hover 加深一档，active 压印（`translateY(1px)` + 投影塌缩）；
    - 运行中 + 有输入 → **拟文**（墨印）：点击走 `sendMessage` 既有**插入路径**（`agent.insertMessage`，下轮生效 + toast 回音）——运行中打字 = 插话，停钮让位。
7. **翰**：命令面板入口钮（设置行右端）——斜杠命令不再只靠盲打 `/` 发现；与 `/` 触发词共用同一层面板，打字接管即散。
8. **运行呼吸线**：任一摊开卷在跑 → 画布底缘 2px **石青**细线呼吸（`.pp-stream-live`，机=石青铁律）；与书脊运行点、坞内停止钮、后台卷指示同源（exec 订阅）。

**2026-09-06 收口（行序 + 模型选择器弹层重做）+ 续批（三行制 + 设置行归堆）**：① **三行制**——书眉行（卷名左端 + 翰/律/后台卷指示右端）恒居坞顶，输入行居中，设置行（模型/权限/思考）垫底——三控件落位「下方一行」（DSH InputBar 排布：输入面在上、控件行在下），不占书眉行（续批打回初版「整组一起下沉」的两行制：卷名/翰/律是坞的书眉，不随三控件迁）；**设置行行内归堆（续批二）**——模型独居左端，权限+思考成对靠右（组内权限在左、思考收尾）：两件同属「运行策略」归堆，不与模型混排；直接合并成单控件暂缓（权限=工作区级 mode-store、思考=每会话 compose-store，两真相源硬合会搅浑状态归属）；② **设置行定宽**——模型 pill 容器 150px / 思考档词 min-width 5em（单字档与两字档同宽），开合、换模型、换思考档不再推挤同行件（卷名书眉行内 132px 定宽防换卷跳动）；③ **模型选择器 compact 形态**——触发器恒驻（打开时仍显示当前模型，不再被空搜索框顶替——「模型被清空」错觉根因），搜索进弹层（搜索行钉顶 + `.ms-listbox` 滚动区），document mousedown 外点关闭（画布空白 mousedown preventDefault 是平移手势，拦得掉 blur——react-aria 的 blur 关闭路永不触发，必须显式听），触发器 mousedown preventDefault 防「blur 抢先关 + click 重开」竞态，再点 = toggle，Escape 收起并回焦触发器；compact 下空选择键忽略——外点/Enter 无匹配的 commitCustomValue 半截查询不落库（自定义模型名提交仍属设置页字段形态）。门禁：tsc / biome(0-0) / vitest 全量（新增 `model-selector-popover` + `composer-dock-row-order` + `composer-dock-header` + `composer-dock-settings-pair` 四文件）。

门禁：tsc / biome(0-0) / vitest（创作坞 6 文件 37+16 用例，含新增 `composer-dock-desk.test.tsx`）/ build:builtin-plugins（23 插件自包含）/ vite build / verify:convergence 全绿。

### 9.1 墨量册（2026-09-13 落地）——坞内 token 计量装置

**来由**：坞是「配置 + 输入」的家，用量是落笔前最该看的读数（还能喂多少上下文）。DSH 同判——上下文表放在输入条旁（ContextMeter 环），不另开页。坞底墨量线继续做余光可见的环境指示，墨量册是它的展开面：**点开看账**。

**落位**：设置行最右端一枚「墨 NN%」触发器（`.pp-ink-sel`——读数件不是控件，不进「权限+思考」运行策略对，独立居行尾；与思考 pill 同语言：纯文字 + 小墨点，>80% 墨点与文字转朱砂）；面板向上开（坞贴屏底），并入浮层互斥（思考/翰/律/引/墨五层同屏只开一个）。

**册面（全出自 `src-ui/src/agent/token-meter/`，真源 = Agent 侧每卷账本）**：
- 书眉：卷名 + 来源徽记（`回报` / `估算`）；
- **读数**：占用百分比当**全册唯一题字**（26px/700），右陪小字距标签「上下文已用」，近满（>80%）转朱砂（句读点朱——全册唯一红字）；
- 副行（mono 小字）：`~已用 / 窗口 tok` + `下一请求预计 ~N`；
- **占用条**：总长 = 窗口占比，段宽 = 构成比——三段按**墨阶递减**（系统提示 `--ink-1` / 工具 schema `--ink-2` / 对话 `--ink-3`），纸墨体系只有墨，不引第三色（石青=运行态、朱砂=人的动作，均不占）；条下一行**构成图例**（色点 + 段名 + `~值`，与条同序同色相）；
- **账目**（段题「账目」）：四桶（未缓存输入 / 缓存读 / 缓存写 / 输出）+ 收口行「合计」（字重上提 + 上弱线）+ 缓存命中率；
- **本轮**（段题「本轮」）：请求次数 + 最新一轮用量；
- 页脚一句口径说明：`~ 本地分词估算 · 账目四桶来自服务商回报`。

**排印（同日二版，用户打回一版「不好看，文字排版搞一下」）**：一版是十三行等重小字竖排 = 设置表单面孔。定稿改为**册页四层**：① 一屏只许一处读数当题字（其余全部退到 10–12px）；② **段题复刻首页节题语言**（8px 实心方墨锚点 + 10px/600/字距 0.26em 题字，浸墨⑤ 完工动作）——两段之间用段题分隔，不用裸分隔线；③ **账目 = dt/dd 两列网格 + 数值右对齐**（账本竖排对齐，眼睛读竖列不读横排；`tabular-nums`）；④ 段内行高 24px、收口行 29px——节奏靠字重与线，不靠等距堆叠。踩坑留档：滚动 flex 列（`max-height` + `overflow-y: auto`）的子项必须 `flex: 0 0 auto`——默认 `flex-shrink: 1` 会把占用条挤成 0 高（肉眼只当「线画得细」，数字自查才逮到）。

**口径纪律**（沿 DSH，禁漂移）：① 分桶**互不重叠**且输入三桶加总恒等于提供方 `prompt_tokens`（残差法归一，不信任各家 miss 字段自洽）；② **压力只算 prompt 侧**（不含输出）——「占用」问的是下一次请求的提示词有多大，不是上一次账单有多大；③ **投影占用** = 压力 + 采样后载荷的带符号增量（流式期间水位站住、跨请求才前进；压缩把载荷削小时同步回落，夹零不出负数）；④ 构成是**估算构成**不是账单（本仓用 gpt-tokenizer 计数通道，与压缩预检同一把尺子）；⑤ 缓存命中率**部分命中绝不四舍五入成 100%**（DSH `formatCacheHitPercent` 的诚实舍入照搬）；⑥ 账本随卷落盘（`.lantai/sessions/{id}.json` 的 `tokens` 字段，旧卷无此字段 = 从空开始，不迁移不编造）。

**归属铁律不变**：创作坞仍是「视图不是容器」——账本真源在 Agent（`getTokenStats/snapshotTokenLedger/restoreTokenLedger`），坞只读；卷级 `sessionTokens` 降为「总量投影 + 重算触发信号」。

**书眉卷名吃满余量（2026-09-13）**：`.pp-composer-target` 原 `flex: 0 0 132px` 定宽把卷名一律截在十来个字，而行内大半空间空着（右端只有翰/律两枚小钮）——改 `flex: 0 1 auto` + `max-width: 64%`：短名不虚占、长名吃到可读上限，换卷/改名仍不推挤右端（spacer `flex: 1` 吸收余量），超长仍 ellipsis。

### 9.2 创作坞浮动化（2026-09-17 拍板：全画布自由浮动 + 吸附边缘 + 双击复位，锚线跟随重算）

**来由**：坞自 2026-09-01 起就是浮在纸上的绝对定位层（不占布局流），但位置由 CSS 钉死（版心居中 + 坐底抬高 96）——坞压住要看的内容时，用户只能挪内容，不能挪坞。

**定案（同日二批修订见下）**：
- **抓手** = 坞的书眉行（卷名/空白处按住即拖；翰/律/停止钮/输入件自己接手势）；
- **拖动** = 自由到画布任意处（夹在顶部浮件带之下、屏缘之内——浮件带见 §14），全程**磁吸**四枚锚位（左右缘 / 版心中轴 / 经典底带 96 / 最底缘）；
- **双击坞头** = 复位到出厂位（撤覆盖、不写死坐标——窗口再变仍居中）；
- **坞位记忆** = localStorage `lantai.composer.pos`（视图偏好，同 `lantai.minimap.pref` 族）；只点不拖不落盘；
- **让位带 `--composer-band`** = 视口底 → 坞顶线（默认位 = 抬高 96 + 坞实测高，与旧 `--composer-h-live` 配对式**零漂移**）。消费面按它重算：小地图默认位（只影响未摆过的默认，用户摆过即尊重）、递牒卡宿主 `.psh-host`、插件 dock `.pw-dock`（后两家经 CSS 吃 `min(--composer-band, --composer-band-cap = 55vh)`）。

**2026-09-17 二批（用户三条实机反馈，逐条改）**：
1. **目次带不让位**（用户裁定「目次带似乎没必要做让位」）：目次带映射区/可见域**只按出厂底带**（`96 + 坞实测高`）算，坞被拖到哪都不压缩导航带——坞是用户自己摆的浮窗，导航带不该跟着缩；「内容尾不藏进坞后」那条 2026-09-01 整改在坞处出厂位时口径未变，照旧成立。消费面**口径分家**：`composerDock: { bottom, height }` 原样下发，让位带 = `bottom + height`（小地图/CSS 让位件），出厂底带 = `96 + height`（目次带）。
2. **拖动与划词打架**：坞上按下会带出**原生选区**，纸上挂着 document 级 `selectionchange`（`use-paper-strips` 的选中浮钮）→ 拖动坞 = 纸上划词 + 浮钮乱冒。修法两层：**锁定态**坞是普通 DOM（根本不接手势，冲突不可能发生）+ **解锁拖动期**清掉既有选区（`window.getSelection()?.removeAllRanges()`，`use-composer-float` 起拖时一次）。~~拖动期在 `<html>` 挂 `pp-composer-dragging` 全页禁选~~——**2026-09-17 收族：全应用默认不可选之后该规则成了空操作，已撤（类保留，服务抓手光标）；冲突的根治上移到 `foundation.css` 选区政策 + 纸壳起手面判据（见 §1 后两条）**。
3. **拖动锁（用户方案，桌面歌词式）**：**默认锁定**——坞对鼠标零响应（不改光标、不接管手势、划词照旧；这同时根治了「谁都不知道这东西能拖动」与误拖）；解锁后**整坞**（除交互件）成为抓手 + 全坞抓手光标。锁态落 localStorage `lantai.composer.unlocked`。
   **二版（同日，用户打回一版「太难看了 + hover 之后点不到」）**：一版把锁钮做成**槽里浮在坞顶外缘的 hover 小钮**——两个病灶：① 浮在坞外与坞之间有缝 ⇒ **揭示靠悬停、命中又要靠揭示（互为前提）** ⇒ 一离开坞体钮先消失，再挪过去什么都没有（用户原话「鼠标还没挪过去就消失了」）；② 浮方块与坞的语言不合（丑）。
   二版 = **归坞的书眉工具行**：`.pp-dock-tools` 里的第三枚 `.pp-tool-btn`（`移` ↔ `锁`，排在 翰/律 之前——那一对内容入口保持相邻），**常显可点、零悬停机制**，样式与 翰/律 同款（纯文字、hover 朱砂、选中态 `.open` 朱砂）；锁态与写面仍在槽主人手里，坞经 `PaperDockContext.composerLock` **能力位**渲染（宿主不给 ⇒ 整枚不出现，只重载单个产物的偏斜窗口不炸）。上夹紧随之回到「顶部浮件带 + 屏缘」（不再为浮钮预留 26px；2026-09-17 标题栏拆除后该值仍是 56，改称 `COMPOSER_CHROME_H`——浮件在右上，坞压上去会盖死窗口钮，见 §14）。
   **教训（入册）**：**控件的悬停揭示区必须与它的可见区同盒**——分离时两者互为前提，必然点不到；宁可常显，不做「脱盒浮现」。

**归属**：坞位/几何/手势/拖动锁全归**槽主人** paper-shell（`composer-float.ts` 纯函数 + `use-composer-float.ts` 机制域）；坞本体（compose-dock）**一字不知**——不给 `PaperDockContext` 加回调、不动宿主面 faceDeps 基线（事件从坞冒泡到槽，槽主人接手势）。旧 `--composer-h-live`（坞自报坞高）随之拆除：让位带是槽主人的几何，一处权威源，浮动态也正确。

**边界（刻意不做）**：让位件之间互不躲让，坞也不自动躲让位件——坞停在上半屏时让位件停在半屏（坞自己负责不遮）；坞不做边框吸附动画；拖动期**不** `preventDefault`（取消 pointerdown 会连带影响 dblclick 跨引擎互操作，复位手势优先）——防误选改由「拖动锁 + 全应用默认不可选（§1 选区政策）+ 划词起手面判据」三层承担。

**过渡期（产物 vs exe 偏斜，2026-09-17 立）**：外壳 CSS（`shell.css` 的 `.psh-host` / `plugin-windows.css` 的 `.pw-dock`）**内嵌在 exe 里、不能随产物热更**，故在「只换产物不重编 exe」的部署窗口内，旧 token `--composer-h-live` 由槽主人 `use-composer-float` 代发一版（新旧两代消费面同时正确）。**拆除条件 = 下一次 `cargo tauri build`**（内嵌 CSS 换成本仓库新式后），届时删该 effect 与 tokens.css 的过渡注。一般形态的教训：**删一个跨层 token 必须与消费它的那一层同批落地**。

**证据**：`tests/composer-float.test.ts`（几何/吸附/拖动面判据/锁记忆纯函数）+ `tests/paper-composer-float.test.tsx`（挂真实 PaperPanel 穿全层：默认锁定 → 点锁钮解锁 → 坞体任意处拖动 → 拖动期全页禁选 → 让位带重算并下发覆盖层消费面 → 松手落盘 → 双击复位 → 交互件不起拖）+ `tests/stage4-toc-top-band.test.tsx`（**坞浮起不让位**：坞位从出厂位挪到半屏，刻痕落点逐字不变；坞高变了才动。该文件 2026-09-17 由 `stage4-toc-titlebar-band` 改名——书眉退役，带体回屏顶，见 §14）。

---

## 10. 浸墨法则（2026-08-31 全局视觉立宪 · 用户拍板 B）

**病灶定案**：全 UI「简陋感」根源 = 只有线框没有墨——古籍隐喻抄了线没抄墨（真古籍版框是粗黑双线 = 全页最重墨，印章是实心朱砂，题字是浓墨大字；产品把这些全做成了灰发丝 + 描边 + 中庸字重）。治法不是加装饰，是**上墨**：同内容同栅格，只改墨量即换血（prototype/ink-ab.html A/B 实证，用户拍板 B）。

**五法则**（token 见 tokens.css「浸墨法则」块）：

1. **墨阶锚点**：重墨全产品只许三处——① 页缘版框（`--rule-frame` 2px 墨线，古籍双栏线）；② 每屏唯一主动作（主钮/拟文印，墨底实色 + `--shadow-anchor` 硬偏移投影）；③ 屏级分区线（首页书眉底线/列表顶线/脚线/坞顶线，`--rule-hard`）。余处禁 2px 硬线。**画布视图的「书眉底线」已随 2026-09-17 标题栏拆除批退役**（画布铺满整窗，该视图的屏级分区线只剩坞顶线；顶部控制件改浮件，走弱线 + `--elev-raise`，见 §14）。
2. **字重极端**（2026-09-01 轻重批对齐拍板 B 钉值测试；2026-09-10 三体换代：MiSans VF 全字重已装载）：题字 `--weight-display` (900)，卷首 700（`.pp-folio-title`），卷名/条目名/动作钮 600，机读注记 400——四级之外禁盘（500 为审计实锤违例已清零）。张力全在两极差。
3. **句读点朱**：题字句号落 `--seal`——古籍句读遗意，每屏至多一处红字。
4. **纸层次**（2026-09-01 真纸化）：全局纹理层（**`body::after` 文档级**，z `--z-grain`；勿挂组件根——`.sh-root` 是首页自己的层叠上下文，纹理会被困在首页，画布/面板全无纹理，实机打回过一次）挂**真纸纹理资产**（`src/assets/paper/paper-grain.jpg` 高频肌理 × `paper-fiber.jpg` 低频纤维晕染，AI 平扫感生成 + 两段预处理：按通道异权提白 `k=R.55/G.40/B.25` 中和黄味（两层 multiply 复利，暖调由 `--paper` carry）→ 统一扩幅 `k=1.6` 放大振幅 ×2.6（提白后振幅只剩四成、画布上读不出纹理，实机打回重调）；`background-blend-mode: multiply` 相乘后随元素 `multiply` 浸入页面，`brightness(1.05)` 校暗度至 ~13%，顶层留 .05 SVG 微颗粒抗色带）+ `--light-fall` 顶部方向光 + `--vignette` 四边微沉 + `--laid-lines` 帘纹（`body::before`）——纸从「底色」变「材料」。印面/重墨受纹理同源于顶层 multiply（全局覆盖），局部禁再挂纹理层——**唯一例外＝印章 `.sh-seal` 挂印泥分材质**（印泥≠纸：`src/assets/paper/seal-paste.jpg` 朱砂膏体颗粒/纤维，同脚本两段预处理成色相中性灰后 `background-blend-mode: multiply` 乘在 `--seal` 实色上，160px 平铺，色随 token 走不锁死在图里）；换纸 = 替换 assets/paper/ 同名图片并跑 `scripts/normalize-paper-texture.ps1`，管道不变。
   - **墨迹洇边（同批）**：重墨大字墨渗——题字 900 加 1.5px 字晕、节题字 1px、节题墨锚 2px 柔晕（`text-shadow`/`box-shadow` + `color-mix(in oklch, var(--ink-solid) N%, transparent)`，同 folio-title 的 alpha 墨残影手法）；发丝线（版框/界栏 1-2px）**不加**——洇边会把细线糊成泥。
   - **流区真纸（材质批二）**：每卷流区＝一张不透明真纸——`src/assets/paper/paper-sheet.jpg`（fiber 线性扩幅回中派生：`a=1.63/b=-.555`——振幅 .031→.067 立得住、均值 .94 不动纸色；原 k=.7 提白档实机不可见；管道 `normalize-paper-texture.ps1 -Sheet`）乘印其上 + `src/paper/sheet.ts` 以卷 id 哈希出纸性三参 `--sheet-ox/oy/j`（同卷恒同纸、卷卷不重样；active 态只动 `background-color`——`background:` 简写会抹掉纸纹层）；流区挂接触落影 `--shadow-sheet`（两段硬偏移堆纸厚——同色纸放同色桌面全靠它读出「一张纸」，非 UI 海拔、是材质物理）。
   - **物理包边（2026-09-02 视觉迭代）**：流区纸缘＝材料不是墨——border/outline 全退役，box-shadow 四层堆出纸的物理四至（受光缘 `--sheet-lit` 左上一线 → 背光缘 `--sheet-shade` 右下 → 裱边带 `--sheet-band` 7px 一圈纸色深带 → 接触落影 `--shadow-sheet` 重校）；剂量 A/B 台 `prototype/edge-ab.html` 定档（亮 .75——米色桌会吃淡光、沉 .13、带 42%）。active 态洗底退役（「变色」判死）——只换落影一档 `--shadow-sheet-active`（手里正拿着的纸离桌面更高一点），活跃信号全走结构墨阶不动纸色；`background:` 简写禁令延续（会抹纸纹层）。
   - **贴纸纹理归属（2026-09-02 透明错觉根治）**：纹理必须随纸走——任何视口固定（fixed）的纹理/纹样层在画布上都是「不动之纹」，拖动时纸走纹不走，材质被大脑判属桌面，流区被读作「透」（实机白纸双盲实锤：纯白实心流区 + 固定横纹 = 仍读作透；流区一直在画，从无渲染故障）。归属三条：① 桌面纹 = `.pp-desk`（`.pp-world` 第一子，世界坐标系随拖动/缩放，四层配方「微颗粒×grain×fiber×帘纹」只混自身层不乘盖流区/纸条）；② 流区纹 = `.pp-region` 自带 paper-sheet 单层（帘纹不进流区——叠乘读作屏纹，实机过审移除）；③ 文档级 `body::after` 颗粒与 `body::before` 帘纹在画布态退役（`body:has(.pp-root)` / `body:not(:has(.pp-root))` 分治），首页/面板照旧（**模态豁免 2026-09-10**：设置面板（全屏模态，墨纱盖画布）在场时恢复文档级材质——工作区里开设置不再被 `.pp-root` 连坐剥光，两场景同观感；实现走「设置遮罩在场即恢复」选择器，画布裸露可交互时材质归属仍按画布态）；`body::before` 画布态只留顶光+边沉——光照类视口固定正当（灯在桌上、不在纸里）。顶眉/坞/目次带等固定家具在画布态无纹（家具与内容分层）。
5. **完工动作**：选中态墨底反白（禁灰底假选中）、浮层必挂投影（`--elev-raise`/`--elev-menu`）、节题前实心方墨锚点。

**转录位**（铺开批次）：首页（已落地）→ ~~画布卷首/流区界栏双线框~~（2026-09-02 翻案：界栏双线框退役，流区改物理包边——见 §10 纸层次）→ 创作坞（prototype/dock-v3.html 已同步浸墨）→ 案卷侧边栏（2026-09-02 落地，见 §12）→ 设置面板。法则改动只动 §10 与 tokens，不许散写。

## 12. 案卷侧边栏（2026-09-02 UX/浸墨批）

八件定案（用户拍板：选中 B 墨洗 / 可调宽 / 保持覆盖 / 缺口全做 / 互斥两态）：

1. **互斥两态**：书脊列 = 侧边栏的**收起态**——左缘任一时刻只留一个。侧栏展开（`left:0` 独占左缘，书脊整列退场）；收起（书脊回场，`sr-slide-in` 回场动画）。canvas-nav 插件以**终态不变量**裁决（并陈 → 书脊退；纸开而两关 → 书脊回），对热替换遗留「双开」态自愈。书脊列**与侧栏同族**（纸底 + 右缘 `--rule-strong`）——两态是同一块板换宽度，不是换了个东西（2026-09-21 重做批补；此前书脊无底无边是散块）。**板面纹理同源（2026-09-21 纸面批）**：画布态把文档级纹理层整层退役（`foundation.css` 的 `body:has(.pp-root)::after`），固定家具随之只剩 flat `--paper`——而旁边桌垫（`.pp-desk`）是有纹的，用户判「纯色背景死气沉沉」。治法 = 两态各挂**同一套板面纹理层**（`.ss-sidebar::before` / `.sr-rack::before`，配方真源 = `tokens.css` 的 `--panel-tex-image/size/blend/bright`，与桌垫逐字同源、**固定 2048 平铺**不走 cover），纹理层 `z-index:-1` 落在面板底色之上、内容之下，`brightness` 只提纹理不连字提亮。**「家具无纹」的例外仅限不透明整块板**——浮在移动纸面上的层仍须无纹（否则以「不动之纹」出卖空间隐喻，2026-09-01 判例）。
2. **空间手势承接**：侧栏行拖放落位 = 书脊手势同族——摊开行拖 = 再落位；闭合行拖 = **先落位再摊开**（region 先写，装载即用落点位）+ expand 自带聚焦。
3. **选中语言**：行型选中（§11 分型）——`--sel-wash` 墨洗 + 朱砂左脊，当前卷与多选勾选行**共用**同一语言（拍板 B：退役旧朱砂洗变体）。
4. **浸墨**：书眉/节头实心方墨锚（收起态换空心框）+ 字晕洇边；机读计数一律**朱字**（seal-deep）；「另起一卷」= 本屏唯一主动作，墨底实色 + `--shadow-anchor`（拟文印同款 hover 翻朱砂深）。
5. **已合卷分桶**：今天 / 7 天内 / 更早三桶（桶头仅多桶时立），**更早默认收起** = 归档语义——大卷量下老卷不删也出视野；节/桶头全可折叠，localStorage 持久。
6. **未存注记**：未落盘新卷行注记出「未存」段（显式标记比缺段诚实——自动存失败可据此发现）。
7. **键盘全席**：↑↓/Home/End 游标、Enter/Space 摊开定位、F2 改名、**C 合卷**、Delete 两击删除、**X 勾选**、Ctrl+A 全选可见、Esc 四级撤退（解武删除 → 解武批量 → 清选择 → 收侧栏回书脊）。
8. **多选批量删除**：勾选格（hover/选中时**替换状态点**显形——一槽一点）+ Ctrl+点击 + X 键；脚部批量条两击确认；运行中卷跳过并报数。行操作钮热区 ≥24px。宽度右缘拖拽 240–420px（localStorage 持久，聚焦后 ←→ 微调 ±16px）。

### 12.1 书脊列（2026-09-21 四批 · 戊「墨缘题签脊」）

定题三条（三轮用户判词）：① **不够好看** + 标签多了**冲出屏幕**；② 对二批修法台的回应——**「并不是按着之前的那些方案改一改就能解决的……得重新设计」**；③ 对三批重设计的回应——**「之前是分块式的，每个卷独立一个标签……是有设计感的。包括『案卷』按钮，做的是一个虚线框按钮，也是有设计感的——现在看起来又平又简陋，设计不存在了」**。

**病灶（四批定位，对拍 git `c78b8c98`）**：判词属实，且不是「没改好」而是**归错档**——§11 海拔表把整列判成「面板架档 = 零投影」，二批据此按「禁框禁影」办，删掉三件语汇：「案卷」扣的 `border: var(--rule-dashed)`、脊块的 `--paper-deep` 底 + `--rule-soft` 框 + `3px --ink-4` 左脊、活跃态的 `--elev-raise` + 朱砂脊边。**脊块是纸片件（与便条/纸条同族），不是面板骨架** ⇒ 归位。

**尺度通则（四批复原台实测）**：照抄 `.pp-region` 的大面积物理包边（受光缘 3–4px / 背光缘 / 裱边带 / 接触落影）缩到 60px 的脊上**读成一片糊**——那套内距是给 720px 流区定的。**大尺寸用纸（渐变带宽），小尺寸用线（有方向的边）。**

六条契约：

1. **函套底**：`.sr-rack` = `--paper` 底 + 右缘 `--rule-strong`——**与 `.ss-sidebar` 逐字同色**（互斥两态真是同一块板换宽度；二批取 `--paper-deep` 实为另一块板）。
2. **脊块 = 纸片件 · 墨缘**：左脊 3px 厚边（常态 `--ink-3` / hover `--ink-1` / 活跃 `--seal`）+ 受光缘（上/左各一线）+ 背光缘（下/右各一线）+ 硬接触落影（`--shadow-sheet`，活跃 `--shadow-sheet-active`）；圆角恒 0，影一律取 token。
3. **整数行**：脊高 = 8px 内距 + N × 栏距；N = `spineWantRows`（3/4/5/6 档，按题名权重）× `allocSpineRows`（列高不够**按行降档**，下限 3 行，不是按比例压缩——旧案四档减内距后是 2.67/3.28/4.10/5.13 行，末行半空或半切）。书缝 6px（块块独立）；列内滚 + 右缘 2px 位置条。
4. **题签**：脊身上另贴一枚亮纸（`--paper` + 同一套受光/背光缘）——两种纸同屏，层次靠纸不靠框。题签宽 39px = 2 × 栏距（恰两栏；42px 会露半截第三栏，比截断更难看）。
5. **脊内三枚小标**（档号 / 枝 / 运行点）全走绝对层，不吃流水高度（档号进流水会在降档时先挤掉题名）。
6. **截字 + 省略号**由 `fitLabel` 给（容量 = 2 栏 × 行数；竖排溢出在块轴上，`text-overflow: ellipsis` 不产省略号）。「案卷」= **虚线扣**（`--rule-dashed`，hover 转石青）。
7. **书口档（乙 · 密度档，2026-09-21 用户拍板）**：`planSpines` 三步——① 自然档（各按需 3–6 行，全放得下就不动它）→ ② 降档档（都压到 3 行下限，13 卷 / 1010px 窗落这里）→ ③ **书口档**（连下限都放不下 ⇒ **活跃卷保持自然高**，其余压成 32px 一条书口）。**书口写题名不写编号**（用户口径：「编号写了跟没写一样」）：横排两行 + 省略号（约 6 字，与整脊竖排两栏同容量、只占三分之一高度），运行点 / 枝 留在左标栏，活跃卷永不书口化。仍放不下才交给列内滚（`--thin-h` = 32 是 TS `SPINE_THIN_H` 的镜像，改必同改）。
8. **热区 = 整块板**：`.sr-rack` 的 `pointer-events` 是 **`auto`**（旧值 `none` 是「无底散块」时代的遗物）。列已是不透明整块板 ⇒ 画布在它下面看不见，让事件穿过去只剩害处：书缝 6px 与四周内距带成了不可命中的真空，滚轮落在那儿不滚列、反去平移/缩放画布（实机读作「滚动失灵」；实测台 `prototype/_probe-wheel.mjs`：脊上滚 0→300 ✓、书缝上滚 0→0 ✗，改 `auto` 后三处皆滚）。

定值：栏宽 **72** = 左内距 6 + 脊宽 **63** + 右内距 3；脊宽 63 = 脊边 3 + 标栏 15 + 题签 39 + 右内距 6（改必同改 `SpineRack.tsx` 与 `tests/paper-c8-spine.test.tsx`）。走查记录：`prototype/spine-craft.NOTES.md`（三批：`spine-redesign.NOTES.md`、`spine-fix.NOTES.md`、`spine-ab.NOTES.md`）。

## 11. 层次法（2026-09-01 立法 · 三轴模型）

来源：全面审计批（生图 mockup 对照 + CDP 全面截图取证，`D:\tmp\ui-audit\`）。层次感的本质 = **让「上面」变得可信**。三根轴朝同一答案发力，只动其一皆塌：**海拔**（谁浮在上面→光影）、**包含**（谁属于谁→线）、**轻重**（谁重要→墨）。

**海拔轴**——梯子全产品四档，token 取值禁散写：

| 档 | 东西 | z-index（§8 立宪） | 影 | 要求 |
|---|---|---|---|---|
| 地面 | 画布/纸/首页 | — | 零投影 | 多数元素住这里 |
| 面板架 | 坞面板根/书脊架**容器**/案卷侧栏 | 280/281/282（`--z-paper-base/rack/sidebar`） | 零投影 | 面板级骨架，恒在地面上、浮起件下（2026-09-01 海拔批补立） |
| 纸内件 | 块/纸条/签/**书脊块** | 纸内 10-60 | 无投影 | 靠遮挡+界栏线+选中线表达 |

> **纸内件无投影**一行是**回归**过的判例：2026-09-05 pin-strip-rework 批把钉住块/纸条当「小号的纸」做（流区四层包边的缩放版 + 接触落影），与本行冲突且实机读不出层次（同色纸放同色桌面，影成了唯一线索 = 「万物皆浮」）；**2026-09-19 便条批**把影收回给流区独占，纸内件改靠**纸色差 + 白边 + 规线**表达（三分材质：桌面有纹 / 流区有纹的纸 / 便条实色无纹）。纸内件唯一配影的时刻 = **拖拽携带态**（`--elev-raise` 压印族，§11 规则① 已列「拖拽幽灵」）。
| 浮层 | 引/思考/斜杠/翰律/模型菜单/状态线下拉 | 45-90 | `--elev-menu` | 必须盖住内容 |
| 模态 | 设置/命令面板/媒体预览 | veil + 400/401/520 | `--elev-raise` + veil | veil 必配（媒体预览加墨幕） |

> **书脊块判例（2026-09-21 四批，勿再归档错）**：书脊**列容器**是面板架档（零投影），但**脊块本身是纸片件**。二批把整列一并按「面板架 = 禁框禁影」办，删掉虚线扣 / 脊块底与脊边 / 活跃影三件语汇，实机判「又平又简陋，设计不存在了」（§12.1）。另有一条**尺度通则**：纸片件在**小尺度**（<100px）上用「有方向的线」表达纸缘（受光缘上/左 + 背光缘下/右 + 硬接触落影 `--shadow-sheet` 族，配一条厚脊边）；`.pp-region` 那套大面积四层包边（3–7px 渐变带宽 + 裱边带）是给 720px 流区定的，缩到 60px 读成一片糊（同批台架实测）——**大尺寸用纸，小尺寸用线**。

规则：① 影分两族按材质隐喻取用（2026-09-01 海拔批修法）——纸片件（模态体/主钮/浮钮/拖拽幽灵/Toast）用压印族 `--elev-raise`（letterpress `2px 3px`，左上光，方向乱则层次死），悬浮面板（下拉/菜单/命令面板）用漫射族 `--elev-menu`（离纸环境软影）；两族之外禁第三种影子 ② 浮层必须盖住内容才配海拔——什么都不盖的回地面 ③ 模态必配 veil（「下面还在但退后了」）④ 档间差必须可见（浮层与模态不可同脸），差在 veil/z/线，不靠影的大小。全局瞬时层（Toast/拖拽幽灵）住 `--z-dialog` 520 带、纹理层 `--z-grain` 之下。

**包含轴**——线是结构语言，分级使用禁倒挂：版框双线（`--rule-frame`，页缘/流区叶框）> 硬线（`--rule-hard`，浸墨③三处）> 强线（`--rule-strong`，界栏）> 弱线（`--rule-soft`，同层细分隔）> 虚线（`--rule-dashed`，草稿/占位）。小事绝不用重线。头线（`--rule-head`，2px ink-2 浮层菜单书眉顶缘——ms-dropdown/sl-log）与语义缘不入分级序列，属判例件。

**嵌套语法**（2026-09-01 包含批立）：① 线随包含级数递减——页面级容器版框/硬线，面板级强线，卡级弱线，行级发丝（`--line-soft`）；外框级数必须 ≥ 内框，外弱内强=倒挂。② 一个方向至多两层框，第三层用界栏/留白/墨洗表达，不立第三框。③ 语义缘不入线级——选中线（2px 朱砂底线/左脊）与警示缘（warn/fail/indigo/pass 左缘 2px）是状态语汇，每件至多一条。④ **泛选择器框禁令**：`XX button` 类泛框必泄漏进嵌套组件（实锤：`.pp-composer button` 强线泛框曾把模型菜单条目全框成卡中卡，特异性压过 `.ms-item` 三边 none）——按钮样式各钮自带，不立泛框。

**轻重轴**——墨阶四档（§3）+ 字重极端（§10 法则 2）+ 语义铁律（朱砂=人/石青=机/石墨=夹注）。上层全对比，下层降对比（veil 压暗、淡墨、发丝线）——印刷品的层次从来靠墨量不靠描边。

**选中语言分型**（2026-09-01 审计定案，细化 §10 法则 5）：锚点型选中（tab/分段主动作）= 墨底反白，或重墨 600 + 2px 朱砂底线；行型选中（列表行/菜单项）= `--sel-wash` 墨洗 + 朱砂左脊。全产品只许这两种，禁第三种。

**致死四动作**（审计实锤，见账本 2026-09-01）：万物皆框 / 万物皆浮 / 散写影子 / 混材质（玻璃≠纸）。

**四问清单**（新面板/组件过审用）：在哪一档？z/影/veil 从 token 取了吗？投影方向与光源一致吗？——它盖住了谁？什么都不盖的，回地面去。

## 13. 目次带 v3（2026-09-14 立法：从「滚动条」到「识别面 + 目次」）

**用途排序（用户拍板，优先级即此序）**：① 精确跳到某一轮/阶段 ② 当前滚动位置的连续控制（滚动条语义）③ 一眼扫出「哪里长、哪里有错、哪里是工具密集段」。

**病灶实测**（实机 CDP，2026-09-14；用户当卷 = 289 刻痕 / 带高 1096px）：289 枚刻痕全做成 button 命中盒（10×8）——tool 刻痕 255 枚把 18 枚阶段锚挤进**平均 3.8px 间距**，命中盒相互叠压、DOM 后渲染者胜：**点自己那枚的比例 7.3%**（268/289 被相邻刻痕盖住），用户读作「刻痕点不中」；289 个 tab 停靠点同样是键盘灾难。另一头：刻痕盒 `top = stripY − 4` 会顶进书眉（＝窗口标题栏，`-webkit-app-region: drag`）拖动带，那 4px 命中归标题栏＝拖窗口（**书眉已于 2026-09-17 整条退役，见 §14**）。

**四层职责分离**（甲，2026-09-14 落地 `b774f4c1`）：

| 层 | 是什么 | 鼠标语义 |
|---|---|---|
| 可点层 | **阶段锚**（朱砂横规；28px 命中盒 ≥ 热区纪律 24px；盒宽只占左缘栏 26px） | 带内**唯一可点目标**——点击 = 飞到该阶段首块；按下锚不启动 scrub |
| 装饰层 | 语义刻痕（tool/plan/error 全部 + 无阶段单元的用户块）：惰性 div + `aria-hidden` + `pointer-events:none` | **不可点**——点它 = 点带空白 = 按位置 scrub（纯扫读信号） |
| 控制层 | 滑块 + 带空白处（VSCode 语义：拖拽 scrub / 点外即跳对中心；夹紧域 = 可见视口） | 连续控制 |
| 识别层 | canvas 墨迹缩略（**乙，待定档**） | 不参与命中 |

两条硬约束：**命中盒与 hover 卡同源**（卡片所示即点击命中——① 锚盒内「阶段 N · 首句」② 贴着装饰刻痕读那枚并提墨一档 ③ 行盒原文）；**映射区永不与顶部家具重叠**——2026-09-14 二报「还是打架」后曾改**结构性修法**：带体 `top: var(--bar-h)` 从书眉下缘起（与标题栏零像素重叠）；**2026-09-17 书眉退役后该下移取消**——带体回到 `top: 0`（屏顶通栏，带体坐标 = 页面坐标），带内一切元素——刻痕盒 / 阶段锚盒（`anchorBoxTop` 贴顶夹紧）/ 滑块 / 未读区 / hover 卡（`cardAnchorFor` 贴顶翻转）——**恒 top ≥ 0**（不越出带体仍是自持纪律；映射区顶 = 刻痕半高 `STRIP_TOP = MARK_HALF`）。

**2026-09-19 加宽批 + 同日甲案（边缘滚动落地之后的误触根治）**：**病灶**——边缘滚动的**右缘感应带**（基准 36px，贴屏最右）正落在目次带上，而带体只 64px 宽 ⇒ 带内几乎处处是感应带：「用带导航」与「贴右缘缘滚」抢同一条带，互为误触（带上的 hover 卡与点带即跳都会在缘滚时被顺手触发）。**裁定（用户「由于边缘滚动的落地，我需要再次加宽我的目次带，因为会有误触的问题」）**：带体 **64 → 104px = 控制列 64 + 缘滚跑道 40**——**控制列**（`.pp-toc-col`，宽度与旧带体**逐字同 64px** ⇒ 刻痕 / 阶段锚 / 滑块 / hover 卡**零漂移**）承载**全部可交互面**；带体右 40px 是**缘滚跑道**（`TOC_RUNWAY_W` ≥ 基准带宽；灵敏度拉满时带宽 ≈51px，只吃掉控制列右缘的 scrub 余量——锚/刻痕只占左 26px，**任何档位都不在感应带内**）。**跑道本体惰性**：无 handler、光标默认、点击只被吞掉（不吞就点穿到纸上划词），而悬停照旧算画布 ⇒ 贴屏最右照旧缘滚（`edge-scroll.ts` 的 `HOVER_ALLOW_DOCKS`）。

**同日甲案（⚠ 2026-09-21 更正归因：此案系 agent 自提，用户从未拍板）**：首版把**识别层也钉在 64px**（图「零漂移」），实机像素量下来带体 105px、墨迹只到 48px ⇒ 右边 57px 读作「带子空了一半」；**改：识别层（墨迹 canvas / 未读区 / 活线）按带体满宽铺**——画布 `clientWidth` 104 ⇒ 剪影横向 ×1.625（48 → ~78px，扫读更清；三者都是 `pointer-events:none` 的纯识别面）。**识别层铺满保留（仍属待用户验收项）**；**该案原附的一道界栏线**（`.pp-toc::after`，`left` = 控制列宽、比带体左缘弱线再淡一档、不吃指针；当初理由 =「看着能点其实不能」是误触的另一面）**已于 2026-09-21 整条摘除**——用户「我从来也没拍板过界栏线」，且该线**零功能**（跑道惰性由组件 `insideCol` 判据决定，与线无关），届时的「用户拍板」措辞属记录失实（更正见 `../plans/paper-shell/taste-ledger.md` 2026-09-21 条）。

**连带与钉值**：顶部浮件右距 80 → **120px**（= 带宽 + 16）。`tests/stage4-toc-top-band.test.tsx` 加宽批两例（几何：组件常量 ↔ CSS ↔ 算术三处同源 + 跑道 ≥ 感应带 + 拉满灵敏度不碰控制件 + **CSS 里不得再有 `.pp-toc::after`**（2026-09-21 摘线后改为反向钉值）；行为：跑道按下不 scrub、不弹卡，控制列照旧）+ 结构断言（墨迹画布是带体子件、不在控制列里——甲案那半的考官）。

**标题栏交互（2026-09-14 三报立法：app-region 全面退役）**：顶部家具/首页顶栏不再声明 `-webkit-app-region`——Windows 下 WebView2 把拖动区**光栅化成一叠薄子窗且只在启动那一刻按当时页面算一次**（启动落在首页 → 按首页 ~88px 的顶栏算），进画布后既多出一条「幽灵标题栏」压在画布顶缘/目次带最上方（拖内容＝挪窗口），又按首页布局吃掉书眉按钮。现在**命中范围由元素自己判定**：`src/app/window-drag.ts`（单一真源）接 pointerdown → Tauri 原生 `start_dragging` / 双击 → `toggle_maximize`，交互件豁免；壳层另有兜底（`src-tauri/src/window_drag_band.rs`：caption 一律降为 client）。2026-09-17 起画布视图的热区 = 顶部浮件本身（书眉退役，见 §14）。**2026-09-18 双击判据修正**：`start_dragging` 是 OS 模态移动循环，第一下就把这一击的 mouseup 吃掉 ⇒ DOM `dblclick` 在 WebView2 上**一次都不产生**；双击改由 `pointerdown` 自数（同点位 ±6px + 450ms 窗口内的第二下，且第二下不进拖动循环），`onDoubleClick` 只作「页面收得到 dblclick 的平台」的同一对点击去重兜底。

**识别层（乙）的物理下限与四个正交通道**：带内缩放比常在 0.005–0.012（长卷 10 万世界 px → 528px 带），此比例下正文 15px 字 = **0.08–0.18px 高**——真字形必然不可辨（v2 退回 bar 的原始理由，见页面原型对照栏）。能携带信息的只有四个**正交**通道，各自的值由原型逐案实测（`prototype/toc-thumb-ab.html`，不入库）：

1. **形状**（行宽剪影）：空行率 0.39→0、墨覆盖 0.44→0.77，与基线 α 差 0.35；
2. **族色**（人=朱砂 / 正文最重 / 程文与工具=石青 / 图表最轻 / 错=`--fail`）：与形状案同形，rgb 差 0.086；
3. **族纹**（表=横纹 / 码=细横 / 图=点阵 / 工具=斜纹）：墨覆盖 0.77→0.68，α 差 0.21–0.32——**不靠色也分得出族**（灰度/打印成立）；
4. **墨量**（形 + 深浅双通道）：α 差 0.05–0.12——答「哪里长／哪里密」。

**淘汰案（同一把尺子判死，勿复活）**：*缩进剪影*——缩进 19 世界 px 在带内 0.2–0.6px，与形状案像素差 0.001（等于没画）；*阶段分带*——阶段界已由可点层承担，缩略里再画一遍与形状案差 0.003（纯重复）。**通则：凡小于带内 1px 的世界结构（缩进、块间留白、单行折行）一律不可辨**——识别层的设计尺度以此为准。

**现状**：甲已落地（`b774f4c1`）；**乙已落地（同日，用户拍板 F ＝ 形状 + 墨量 + 族色 + 错）**——纯层 `paper/toc-ink.ts`（`buildTocInkBuckets`：桶高 3px、alpha = 0.18 + 0.82×√(桶内墨面积/全带最大)、a 桶内最宽行定宽并定族色、错旗另立 `--fail` 4px 短规），组件只负责把桶画出来（族色走 `inkBarColorOf`、错走 `INK_FAIL`——canvas 取不到 CSS 变量，故 `--fail` 在 `paper/ink.ts` 有字面量镜像）。钉值 `tests/stage4-toc-ink-layer.test.ts`（8 例，含**差分断言**：同一份数据下 v2 规则覆盖 9%、新算法 ≥80%；算法退化即红）。**族纹（D）未采纳**——留作灰度/打印场景的后备通道；v2 的块级降级阈值（`DENSITY_BLOCKS`）随本批拆除。

## 14. 顶部浮件（2026-09-17 标题栏拆除批）

**病灶（用户实机，边缘滚动落地之后）**：「书眉整条是 56px **布局行**」——它把画布顶缘从窗口顶推开 ⇒ 边缘滚动最自然的动作（**把指针甩到屏顶**）永远落在书眉上：悬停档判据要求指针落在画布内（`hoverEdgeEligible`），屏顶那 56px 不是画布，一律否掉；拖块贴顶同此（越出画布封顶也只到 56）。**上缘在用户视角里等于没有边缘滚动**——这不是带宽/灵敏度问题，是布局问题。

**裁定（用户「按钮从标题栏挪出来，然后把标题栏删了」）**：书眉布局行**整条退役**，控制件落成右上**一枚浮件**（`.pp-chrome`）。

| 项 | 定案 |
|---|---|
| 画布 | 铺满整窗（`.pp-canvas` 独占 `.pp-root` 的 flex 列）——**顶缘 = 屏缘**，四缘感应带对称 |
| 浮件内容 | `画布`（抓手）+ 缩放 −/读数/＋ + 状态字（StatusLine）+ 设置 + 回首页 + 窗口钮（WinControls）——**搬家不是删除**，相对次序与旧书眉一致 |
| 落位 | `top: 8px; height: 40px; right: 120px`（= 目次带宽 104 + 16 呼吸；带宽 2026-09-19 由 64 加宽，见 §13）；**带高 56 = 8 + 40 + 8 呼吸**，坞上夹紧与小地图默认位同此值（TS 镜像 `COMPOSER_CHROME_H` / `TOP_CHROME_H`） |
| 宽度 | 随内容浮动（右对齐）——状态字在本批同批收窄（`.pp-chrome .sl-chip` 上限 320 → 220px，全文照旧可点开读）；典型宽约 470px，最长约 560px。**顶缘感应带只在浮件占位那段 x 区间失效**，其余整条可滚 |
| 语言 | 纸底 + `--rule-soft` + `--elev-raise`；**不透明**（半透明会让画布正文与浮件文字互相糊字，同 §10 纸层次与「坞底 / HUD 化」诊断）；**常显**，不做悬停揭示（揭示区与命中区一分开就互为前提，先例＝拖动锁一版） |
| 层级 | 覆盖件（`.pp-canvas` 的**兄弟**，不进布局流；**不是画布 DOM**——故悬停其上不滚，同目次带之外的覆盖件口径），z 30（旧书眉同档） |
| 窗口拖动 | 热区 = 浮件本身（非交互件即 `画布` 二字与件间空白），走 `app/window-drag.ts` 原生通道；画布视图**不再留整条拖动带**（否则又变成「屏顶不能滚」）。首页顶栏 `.sh-head` **保留**（首页不是画布，无「顶缘即屏缘」诉求） |

**连带（几何同一批收口）**：目次带 `top: var(--bar-h)` → `top: 0`（带体坐标 = 页面坐标，`TOC_TOP = 0`）；书脊架 `.sr-rack` / 案卷侧栏 `.ss-sidebar` 的 `top: var(--bar-h)` → `0`（左缘整列多出 56px）；一次性眉批 `.pp-hint-canvas` 从「书眉下缘 + 20」上提到「屏顶 + 20」；`tokens.css` 的 `--bar-h` **删除**（已无消费面）。**几何刻意留在产地域**（`.pp-chrome` / `.pp-toc` 的数值在插件 CSS 里）：tokens.css 属壳域（`main.ts` 打进 bundle），改它必须重建 exe，而本批要的是「只换产物热更」（`docs/dev-workflow.md`）。

**已知边界（刻意不做）**：① 浮件本体之上的悬停**不滚**（它是「别的面」）——故顶缘感应在浮件占位的那段 x 区间里不可用；若日后要「浮件背后照滚」，把 `.pp-chrome` 加进 `HOVER_ALLOW_DOCKS` 即可（三行，判据已就位）。② 目次带（右缘）与案卷侧栏（左缘）本就压着左右感应带，各自的口径见 §13 与 `edge-scroll.ts`。③ 坞顶仍不得进浮件带（上夹紧 56）——浮件在右上，坞压上去会把窗口钮（decorations:false 下唯一关闭入口）盖死。

**钉值**：`tests/stage4-toc-top-band.test.tsx`（带体从屏顶起 + 浮件几何四处同源：CSS `8/40/80/64` ↔ 两处 TS 镜像 56 ↔ 测试基准）；`tests/paper-viewport-ux.test.tsx` +3（结构：书眉不存 / 浮件是画布兄弟 / 控制件一件不少；行为：屏顶那 36px 落画布上滚 vs 同一姿态落浮件上不滚；浮件抓手拖窗口而按钮不吃拖窗口、双击最大化）；`tests/edge-scroll.test.ts`（判据树照真机结构加 `.pp-chrome` 兄弟项 + 新增「上缘感应带在屏顶」用例）；`tests/titlebar-drag-region.test.ts`（`pp-topbar` 零声明 + 浮件接共享实现）；`paper-visual-decisions`（墨阶锚点：画布书眉底线退役，浮件走弱线）。
