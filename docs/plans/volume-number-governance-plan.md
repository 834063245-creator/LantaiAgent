# 卷号治理（A 收显示 + B 号不复用）· 施工单

> 立项 2026-09-21。触发：用户两问——「我的每个会话都必须有一个编号吗？」→「这个世界上根本没有
> Agent 软件会到处显示会话的编号。UUID 似乎更优？」
> **用户拍板：A + B**（C「身份换 UUID」不做——B 基本拿到 C 的收益，代价小一个数量级）。
> 取证：两路只读审计（显示面全表 / 发号装置与风险路径）已并入本件，结论逐条带 `文件:行号`。

## 0. 裁定（决定放前面）

| # | 裁定 | 理由（一句话） |
|---|---|---|
| D1 | 身份仍是**数字卷号**，不做 UUID | 号已当主键用在四处（文件名 / 血缘 `parent`+`atSeq` / 每卷账本 / 检索手柄）；换 UUID = 二三十个文件穿透 + 丢掉「案卷 12」这个可念手柄 |
| D2 | **号一经发出，永不复用**（发号账落盘） | 复用是整个「死卷冒充新卷」bug 族的根（真机事故见 §1）；拔根 ⇒ 那一族从「有」变「结构上不可能」 |
| D3 | 发号账**独立成文件**，不寄生 `_index.json` | `_index` 是**可丢弃投影**（版本不认即整份重建、惰性载入、防抖 fire-and-forget 写）——单调事实不得住在会被重建的缓存里（单一权威源） |
| D4 | **有名卷**的常显号收成两处：书脊档号 + 卷首眉行 | 其余位置都是「名字旁边再报一次号」的重复；书脊的号是「题名两栏截断后唯一可靠的身份」（守护测试原文），卷首恰一次是 2026-09-16 用户拍板 |
| D5 | A 只改**呈现面**，不动数据结构；**无名卷的号即其名**（兜底显示全留） | 退掉的是**重复显示**，不是身份——号继续存在、继续可按号检索、继续进诊断文案；无名卷没有名字，号是它唯一标识 |

**命名说明**：本篇沿用「卷号」（机读注记与代码里的 `id`／`sessionNum`）；它在 UI 上的读法是「档号」，
但那是**呈现层的说法**，不是新的实体。术语不新增。

## 1. 现状与病根（证据）

**发号器**：`src-ui/src/state/session-store.ts` 的 `nextSessionId`（纯内存，初值 1）。

| 面 | 位置 | 语义 |
|---|---|---|
| 起卷占号 | `ui/chat-session.ts:594` | 任何 `await` 之前占号（并发双回车不铸同号；占号不回退，epoch 丢弃也烧号） |
| 立枝取号 | `app/chat/session-branch.ts:452-455` | `max(内存号, 磁盘最大号+1)`——「宁可多发一号也不赌」（撞号 = 原子替换写覆写既有卷） |
| 重启对账 | `ui/chat-session.ts:1471` | `max(内存号, 磁盘最大号+1)` ⇒ **重启回退**：删掉高位卷后，那些号会被重新发出 |
| 摊开抬下限 | `ui/chat-session.ts:1741` / `:1878` | 续开/批恢复大号卷后抬内存下限（不落盘） |

**复用为什么是真病灶**（不是洁癖）：`docs/plans/session-tree-plan.md` §12 记了真机事故——上一代死卷残留的
`{id}.json` 正好接住今天新发的同号卷，三个子卷**顶着 9/6 死卷的名字**且不会自愈。为此今天长着三台机器：
读面「同代校验」（`ui/chat-session.ts:1480` 起）、已删卷写拒绝名单（`_removedVolumes`）、立枝的「宁可多发一号」。
**这三台机器的存在理由，全是「身份是可复用的小整数」。**

**先例**：退役的总目 `_ledger.json` 里本来就有这一栏（`docs/archive/session-ledger-plan.md:56-76`：
`"nextSessionId": 231` +「发号 = max(总目.nextSessionId, scanMaxSessionId(dir)+1)」）。总目因**摊开集**用途退役
（`ui/chat-session.ts:1258`），**发号这一半本批补回**——形状照抄，用途收窄到发号。

**本批顺带修的雷**（审计新发现）：`session-branch.ts:452` 在 `await scanMaxSessionId()` **之前**取了 sess
快照，`:454` 读的是陈旧值 ⇒ **并发两次立枝可铸同号并互相覆写**（`write_log` 是原子替换写）。`createNewSession`
2026-09-09 已修过同族问题，立枝漏了、且零测试覆盖。本批取号重排后**同步占号**，雷随之拆除（回归用例见 §4）。

## 2. A：显示收口（全表 + 逐项裁定）

| # | 位置 | 现状 | 处置 |
|---|---|---|---|
| 1 | 侧栏行机读注记 `session-sidebar-model.ts`（`sessionMeta`） | `Nº 12 · 8 块 · 1 小时前` | **退 `Nº`** → `8 块 · 1 小时前` |
| 2 | 书脊 hover 小卡（`SpineRack.tsx`，同一 `sessionMeta`） | 同上 | 随 #1 一并退 |
| 3 | 血缘牌/行 title + aria（`SessionSidebar.tsx` 三处） | `枝（父卷 Nº 15）` | **退号**：title 只留「枝」+「自父卷分出」 |
| 4 | 血缘卡机读行（`SessionSidebar.tsx`） | `Nº 15 · 8 块 · 枝自它分出` | **退 `Nº`** → `8 块 · 枝自它分出` |
| 5 | **创作坞书眉 hover**（`ComposerDock.tsx`） | `案卷 ${id}`（**对有名卷也硬报号**，且裸用 id 绕过 `volumeDisplayName`） | **退号** → hover 出显示名（单一真源），与同元素可见文本一致 |
| 6 | 卷首枝边引线 aria（`PaperPanel.tsx`） | `回到案卷 ${childSid} 这一枝的来处…` | **退号** → 「回到这一枝的来处：父卷的分叉节点」 |
| 7 | **血缘卡「不在场」标题**（`SessionSidebar.tsx`） | `案卷 Nº 99（不在场）` | **改形态**：走 `volumeDisplayName('', id)`（号留在**名字位**，不重复报） |
| 8 | **卷首眉行**（`PaperPanel.tsx`） | `案卷 Nº 12`（有名卷） | **留**（`docs/design/lantai-design-spec.md:23` 拍板「恰出现一次」；档行已无号） |
| 9 | **书脊档号小标**（`SpineRack.tsx` `.sr-num`） | 每条脊恒显 `12` | **留**（守护测试原文即理由：题名两栏截断后唯一可靠身份——**索书号**用法，独立规格 `design-spec:334`） |
| 10 | 无名卷显示名兜底（`state/volume-name.ts`） | `案卷 N` | **留**（D5；消费面 = 侧栏行名 / 卷首题字 / 书脊题名 / 墨层地志标签 / 出处行 / 窗口标题 / 墨量册标题 / 权限卡徽标 / 案头签条） |
| 11 | 检索命中（`session-sidebar-model.ts` `filterRows`） | 按名或**按号子串**命中 | **留**（隐形能力；placeholder「卷名或卷号…」的承诺不变） |
| 12 | 诊断/toast/状态字（`persistence.ts`、`SessionSidebar.tsx` 批量删除报数、`session-branch.ts`、`session-log-store.ts`、`chat-session.ts` 删除/发号账失败等） | `案卷 12 未落盘…` | **留**——这些调用点手里**只有 number 没有 label**（`running: number[]`、`failed[].id`），退号 = 报错说不出哪一卷 |
| 13 | 审计日志 `{工作区}/.lantai/logs/ui.log` | 卷号进 NDJSON 日志 | **留**（排障面，与界面显示无关；本表显式登记） |
| 14 | 导出面（`chat-session.ts` 导出 md 头/文件名）、模型可见工具输出 | **零卷号**（审计负结论） | 无需动 |

**口径（修正版）**：**有名卷**的常显号从三类面（侧栏行 / 书脊 / 卷首）收到**两类面**（书脊 / 卷首）；
**无名卷的号即其名**，随兜底显示出现在各处（这是 D5 的必然结果，不是漏网）。

**已知代价（诚实栏）**：退掉侧栏行注记后，按号检索仍命中，但**行上不再显示号** ⇒ 用户输入 `12` 得到一行
「Cordis 迁移」，匹配理由不可见（现无高亮机制）。接受：placeholder 的承诺是「能搜到」，不是「看得见号」。

**既存漏网（记在册、本批不修）**：小地图卷框 hover 的 `title={v.label}`（`paper-minimap/MinimapView.tsx`，
数据由 `use-paper-regions.ts` 给**原样 label**）**没走** `volumeDisplayName` ⇒ 无名卷 hover 是空串。
退卷号对它零影响；修它反而是**增加**一处显示（无名卷的号即其名），故不属本批方向，另案处理。

**受影响测试**（规格变更，显式声明，不偷跑）：`tests/canvas-nav.test.ts`（`sessionMeta` 期望串）、
`tests/session-sidebar-tree.test.tsx`（2 处 title 断言）、`tests/session-sidebar-views.test.tsx`（title + 卡机读行）。
`tests/paper-c8-spine.test.tsx` 的「档号恒显」**不动**（#9 保留）。

## 3. B：发号账（`_issue.json`）

**owner**：`src-ui/src/ui/chat-session.ts`——卷号生命周期的唯一 owner（发号点与对账点都在此，且
`_volumeCatalogs`/`_removedVolumes`/每卷写链三个同族模块态也在同文件）。不新建模块：新模块只会多一条
import 边（`ui/chat-session` ↔ `composition` 成环事故见 `:1770` 注），收益为零。

**文件**：`{工作区}/.lantai/sessions/_issue.json`，形状 `{"ver":1,"next":22}`。走下划线保留名 + 既有 seam
（`read_volume` / `save_volume`，与 `_index.json` 同款）——**零 Rust 变更、零 RPC 契约变更、零格式版本升级**；
卷集判定只认 `.ndjson` 且跳过 `_` 开头，故账文件对既有清点面天然隐形。

**语义**：`next` = 下一个可发的号 = 已发出的最大号 + 1，**单调只增**。

```
读（每 root 每进程一次；坏档/缺席 ⇒ 当 0 + warn 可见）
对账 装配 / 立枝：next = max(内存下限, 磁盘最大号 + 1, 账 next)   ← 这两处本来就要扫盘
     起卷（发号路径）：next = max(内存下限, 账 next)              ← 不扫盘（见取舍 2）
发号 ensure（await 读账）→ 同步占号（claim→setState 之间零 await）→ 写账（await）
```

**五条关键取舍**：

1. **写账要 await（不是 fire-and-forget）**：否则「发出号 → 立刻崩溃 → 该卷被删」仍能复用——账的全部意义
   就是关掉这个窗口。写账发生在**卷物化之前**（先记账再建卷 ⇒ 崩溃只烧号、绝不回收）。写失败**可见**
   （warn + toast 一次），不静默、不阻断建卷。
2. **对账取 max，账不参与回退**：`scanMax + 1` 这一项**长期保留**（手工拷入的卷 / 账丢失 / 旧盘），
   但**只在对账时本来就要扫盘的两个入口参与**（装配 / 立枝）。**发号路径不扫盘**——起卷今天只认内存
   （「发号同步占位」纪律），盘上号由装配对账与开卷抬下限覆盖；把目录枚举塞进发号会改变既有语义
   （2026-09-21 全量回归实测：9 例因它变红，收窄后零漂移）。账那一项则每次都在场，且账读取每 root
   每进程一次 ⇒ 第二次起发号零 I/O。
3. **版本不认的账，定向拒读、绝不覆写**（同卷日志格式纪律）：盘上是更新版兰台写的账 ⇒ 本 build 只 warn
   + 退回磁盘对账，**不写它**——覆写会抹掉新版已发出的号段，降级运行反而制造复用。
4. **内存键归一**（`D:\ws` 与 `D:/ws` 是同一份盘上账；`workspaceSessionsDir` 只剥尾分隔符、不转反斜杠）
   ⇒ 两条键各持一份水位是错的。
5. **旧工作区不迁移**：无账 = `next` 记 0，首次发号时补写账；历史已删卷的空号**不追溯**（与今日
   `scanMax+1` 的起点一致）。

**INVARIANTS 对齐**：#1——模块态 `_issueLedgers: Map<root, …>` 键控自清理（与 `_volumeCatalogs` 同族，
声明处注明归属）；#12——写账是逃逸所有权的在途写 ⇒ 沿用 `getWorkspaceEpoch()` / `isCurrentEpoch()` 代际校验
（对账入口内建）。另：`setNextSessionId` 访问器改为**只增**（旧语义可拉低水位 = 直接拆 D2 的承重面）。

**不做（本批范围外，另立）**：不拆 `_removedVolumes` 与同代校验（账让它们从「必需」降为「冗余保险」，
拆要另一份证据）；不收口「卷集判据三份手抄」（`chat-session.ts` 两处 + `chat-core.ts`）；不动 Rust 侧
`scan_sessions_dir` 的 `.json` 计数口径（与 TS 卷集不是同一把尺子，属既有发散）。

## 4. 测试计划

**新增** `tests/volume-issue-ledger.test.ts`（9 例，全走真入口；起卷例按**生产序列**——先装配对账再起卷）：
1. 起卷即记账：账 `next` = 新号 + 1；
2. **号不复用（核心，修前红）**：账 `next=20`、盘上只剩 1..5 ⇒ 重启（模块态清空）后起卷拿 **20**（修前 = 6）；
3. 立枝同源：同场景走 `createBranchVolume` 也拿 20；
4. 账坏档 ⇒ 退回磁盘对账并重建账（不炸）；
5. 账版本不认 ⇒ 退回磁盘对账且**账原样在场**（绝不覆写）；
6. 账缺席（旧工作区首开）⇒ 退回磁盘对账 + 首次发号补写账；
7. 账不回退（磁盘下限 > 账 ⇒ 取大者并把账抬高）；
8. 账落盘失败不阻断建卷（可见 warn；号照发）；
9. **并发立枝不铸同号**（§1 那颗雷的回归钉子）。

**修前红实测**（2026-09-21，备份/还原法）：同一份用例在改动前 **8/9 红**（含并发立枝），改后 9/9 绿。

**规格变更（显式声明，同批改）**：§2 表列的 4 个测试文件断言 + 三处**夹具随新语义更新**——
`chat-epoch-guard.test.ts`（T0 静态断言改钉新 owner `reconcileVolumeIssueFloor`）、
`audit-fixes.test.ts`（导出面的「只写一笔」断言剔除发号账那一笔）、
`chat-send-liveness.test.ts` / `session-unify-u3.test.ts`（`beforeEach` 补 `resetSessionListCacheForTests()`：
这两文件的每个用例 = 一个**全新工作区**，而号现在是工作区级单调的）。
**零改动面**：档号恒显、检索命中、卷名派生全链、诊断文案、导出面、起卷只认内存的既有语义。

## 5. 门禁与验收

| 面 | 命令 |
|---|---|
| 逻辑 | `cd src-ui && npx vitest run`（改动文件先单跑） |
| 构建 | `cd src-ui && npm run build` |
| 格式 | `npx biome check --write <改动文件>` → `npx biome ci .` 0/0 |
| 文档 | `npm run doc-check`（本件进索引；`--report` 看漂移） |

**不需要** `verify:convergence`（不碰 `src-ui/src/agent/**`、`src-ui/src/composition/**`）；**不需要** cargo（零 Rust 改动）。

**验收（结果论）**：① 用例 2 修前红、修后绿（B 的唯一硬证据）；② 用例 9 修前红（并发立枝同号）；③ 删高位卷 +
重启后新卷号 > 所有已删号（真机手测一次）；④ A 面截图对拍：侧栏两行式行只剩「名 / 块数·时间」，书脊与卷首的号各自仍在。

## 6. 风险与残余（诚实栏）

| 风险 | 现状 |
|---|---|
| 账写失败（磁盘满/被杀） | 可见 warn + toast；该窗口内号仍可能复用——账不承诺防「账本身写不进去」 |
| **工作区移除 / 手工删会话根**（`workspace.rs:247-255` `remove_dir_all`） | 账与卷**同生共死**（语义自洽：卷都没了，号无从复用）；但删根后重开同一路径 ⇒ 从 1 重发。**这是账关不掉的残余** |
| **多窗口/多实例**（未发现单实例守卫；`write_atomic` 无 CAS） | 两实例都读 `next=20` 都写 21 ⇒ 都发 20。今日同样撞号（两份内存发号器），**账不使情况更坏也不修好**；进程内「两 panel 同 root」的读-改-写交错由「同步占号 + 每 root 一本内存账」防住（生产暂单 `ChatCore`） |
| 工作区被拷到另一台机器 | 账随工作区走，号段一致；同时开两台 = 上一行同险 |
| 版本升级 / 手工删账 | 定向拒读或退回 `scanMax+1` 并 warn——比今日不劣，不炸 |
| 同代校验/已删卷名单变成冗余 | 本批**不拆**；账稳定跑一段后另立「拆冗余保险」评估（需要证据：账在场时那些路径是否真的永不触发） |

## 7. 终止条件

A 面 §2 全表落地 + B 面 §4 用例全绿 + §5 门禁全绿 + 文档写回（本件、`docs/plans/README.md` 索引、
`taste-ledger.md` 一条），**且**真机跑一次「删高位卷 → 重启 → 起卷」确认号不复用。达成即竣工归档。

> `HISTORY.md` **本批不写**：它自 2026-08-29 起未再维护（近三周的里程碑都没进），单插一条 09-21
> 只会留下一个假缺口；本批的拍板已落在 `docs/plans/README.md` 索引 + `taste-ledger.md` + 本件三处。
