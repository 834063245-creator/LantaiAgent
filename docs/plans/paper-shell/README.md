# 白纸壳（paper-shell）——前端重构主计划（独立工程）

> **本目录阅读顺序**：① 本 README（工程定位 + 管线 + 待定清单）→ ② `interviews/`（按日期，最新拍板为准）→ ③ `taste-ledger.md`（视觉决定账本，V2 起持续记账）→ ④ `walkthrough.md`（走查弹走查指南——待用户判定）。设计原理在 `docs/design/一张纸-Agent软件交互形态设计.md`（注意 §2.3 有界画布已被 R1 修订为无限画布+方位感，见 interviews/R1-2026-08-20.md D-R1-1）。

> 立项：2026-08-20 · 状态：**Active（2026-08-21 走查弹毕业——用户判定「感觉是对的」（判定记录见 walkthrough.md）；下一步 V3a 骨架内核开工。V1 视觉暂停于两轮负结果，见 taste-ledger。走查涌现新课题：块粒度与截取，见待定清单 #10）**
> 设计地基：`docs/design/一张纸-Agent软件交互形态设计.md`（§1-5 已定：卷轴隐喻 / 两层所有权 / 块协议 / 三层分离 / 视觉语言；§6 为待决清单）
> 原型资产：`prototype/` 8 个 HTML 原型 + 4 张预览图（见 §资产盘点）
> 组合层关系：`composition-architecture/`——纸壳最终装配在组合层上（V3b，gate 于其 S1），但那是**装配关系，不是排程关系**
> **定位（2026-08-20 修订）：这是与组合层并列的独立大工程，不是它的一个阶段。** 前端重构 = 创作工程（密集人机共同创作：沟通→修改→测试循环，周期以月计）；组合层 = 机械工程（契约清晰、gate 可验、绿灯模式）。两者物种不同，各自成独立计划。

## 本工程的性质（写给所有 agent——先校准预期，再谈管线）

**前端不是「一切定好就能做好」的转录流水线。** 定稿的契约（V2）只消灭「随机重掷」这一类失败；定稿之后仍有大量工作，形态是**高频率的沟通-修改-测试循环**：用户在真实使用中产生不满 → 描述不清的模糊反馈 → agent 猜测意图出候选 → 用户再判。这个循环是前端工程的**本体**，不是开销——观测台的现状就是非常多轮这样的循环磨出来的。本计划对它的态度：不消灭循环（消灭不了），而是给循环上纪律（V4 防发散协议：一环一维、对照反馈、账本、止损），让每轮循环都产生**留存的知识**（品味账本），而不是蒸发。

用户角色因此不是「短会话点缀」而是**共同创作者**——本工程的主要生产资料之一就是用户的判断力。给 agent 的排程含义：paper-shell 的推进节奏由用户对话带宽决定，agent 不催促、不擅自跳过人判环节、不用「契约已定」为由把循环压扁。

## 方法论：把「发明」压缩到最小，其余交给循环

| 任务类型 | Agent 能力 | 在管线中的合法位置 |
|---|---|---|
| 发明视觉设计（从构想产出新视觉） | ✗ 弱 | 只发生在 V1 一次性原型里；**人是唯一审美仲裁**（看 + 选） |
| 转录视觉（把已批准的视觉翻成代码/token） | ✓ 强 | V2 契约编码 + V3/V4 生产代码——**只许转录，不许现场发明** |
| 骨架工程（数据模型/布局数学/虚拟化/协议） | ✓ 强 | V3a 壳无关内核（纯逻辑，零视觉判断） |
| **共同创作循环（模糊反馈→候选→人判）** | **人机各半** | **V4 全程 + V5 之后的产品常态**——这是本工程的主体工作量 |

证据：`prototype/` 目录就是这条环路的非正式跑通——`paper-prototype-light.html` 已含完整 token 系统（desk/paper/folio/ink/brass 全套 + Fraunces/Noto Serif 字体对 + 动效曲线），`ink-brass-scene-v2.html` 是一个自洽的「墨与黄铜·观测日志」静物方向。视觉方向不缺，缺的是收敛决策、正式契约和骨架工程。

## 资产盘点（2026-08-20 实查）

| 资产 | 状态 | 去向 |
|---|---|---|
| `paper-prototype.html` / `-light.html` | 双色打样：token 系统 + 纸中纸格线 + 块拖出钉住演示 | V1 收敛轮起点 |
| `ink-brass-prototype.html` / `-scene-v2.html` | 墨与黄铜静物场景（观测日志美感：卷册/压纸/墨表） | V1 候选视觉方向 |
| `layout-prototype` / `pill-demo` / `toolbar-demo` | 布局 / 胶囊 / 工具条局部探索 | V2 组件清单输入 |
| `demo.html` / `observatory-concept.html` + 4 预览图 | 旧观测台方向 | 仅参考 |
| `src-ui/src/lib/pretext` | Canvas measureText 排版测量（设计文档 §3.3 引用，**仍在**） | V3a 测量引擎 |
| 设计文档 §6 清单 | 7 个待验证/待决策 | §问题分配表 |

## 管线

### V0 — 愿景外化访谈（每轮 ≤4 问，小时级）

- **R1 ✅（2026-08-20 已毕）**：D-R1-1 无限画布 / D-R1-2 默认 flow / D-R1-3 流锚甲 / D-R1-4 不可逆五条 / D-R1-5 主题化（见 interviews/R1）
- **R2 ✅（2026-08-20 已毕）**：D-R2-1 拖出钉住+动手权 / D-R2-2 按钮确认收回 / D-R2-3 内推外拉+活引用 / D-R2-4 横轴开放主权区（见 interviews/R2）
- **R3（未开；V5 前拍板即可，不挡任何前期阶段）**：迁移与退役——共居期长度、观测台退役判据
- 产出：本 README 相应节填满 + §6 清单逐项消账

### V1 — 原型收敛轮（每轮小时级，一次性 HTML）

每轮回答 §6 的具体视觉问题：**出 2-3 个变体 → 人看 → 选/混**。变体从既有原型派生，不重起灶。~~已可开的第一轮：白纸 vs 深色~~ **第一轮已被 D-R1-5 改题**：白纸/深色不是二选一（同前端两主题）——首轮改为 ① 主题双套并排对照（验证 token 双套机制可行）② 写字段落格线变体（§6 项）。

纪律：原型是消耗品——原型代码**不许**进生产（进生产是 V2 转录的事）；每轮只回答清单上的问题，不顺手探索。

### 走查弹（tracer bullet，R1 已确认定义）— V1 之后、V3a 之前

挂在观测台旁的纸视图：真实会话数据 → 灰框块（markdown + diff + tool result）→ 无限画布（原点十字）→ 流锚甲（最新块贴下缘、输入条固定底部）→ 可平移/缩放/钉住一块。三条件：**走真实数据（不 mock）/ 穿全层（转译→数据模型→画布→流锚，零跳过）/ 丑得理直气壮（视觉全不管，结构对即可）**。成功标准：用户说出「对，流就是这个感觉」或「不对，卡在 X」。

**落地状态（2026-08-21，五轮工程 + 9 轮 CDP 冒烟全绿）**：

- 实现：`src-ui/src/paper/`（headless 内核：块模型/转译/画布数学，23 无头测试）+ `src-ui/src/app/panels/PaperPanel.tsx`（全屏壳，用户拍板可遮主视图）
- 开合：`Ctrl+P` / 命令面板「纸视图」/ `Esc` 关闭（panel-def `paper` 行）
- **走查指南**：见 `walkthrough.md`（本目录）——五步走查法 + 已验证矩阵 + 判定记录表
- **判定后去向**：「对」→ V3a 开工（本计划下一段）；「卡在 X」→ X 直接变 V3a 第一张工单

### V2 — 视觉语言编码（1-2 天，纯转录）

赢者原型 → `paper-tokens.css`（token 块在原型里已存在，照抄成正式文件；**主题机制**——token 双套 paper/dark 同构同义不同值 + 运行时切换 + 跟随系统/手动 + 降级规则，D-R1-5）+ 组件清单（块渲染器类型 / 材质 / 投影 / 动效曲线 / **每类块的「可动手作集」**——渲染器必须回答「用户能对它做什么」，工作量预判不小，D-R2-1）+ 金样截图若干张。

**这是视觉契约**：此后一切生产代码的视觉只从这里取。评审试金石：「这段视觉是契约里的吗？」——不是，退回。

### V3a — 骨架内核（壳无关，可与组合层 S0-S2 并行；agent 主场）

> 走查弹（2026-08-21 毕业）已提前交付了 V3a 的第一批真文件：`src/paper/`（块模型/转译/画布数学/流锚）+ PaperPanel——V3a 在其上展开，不是从零开始。

- 块数据模型：`{ id, 类型, 内容, state: flow|pinned, 世界坐标 x/y/w }`（设计文档 §3.1 真相层；state 默认 `flow`——D-R1-2；**钉住块 = 活引用非拷贝**，存储挂文件源、渲染层是视图——D-R2-3；**世界坐标唯一真相**——D-R2-4）
- **块粒度与截取**（待定 #10 并排原型人判后定型）：段落级拆块 / 选区拖出 / 混合三路——选区路径依赖逐行布局原语（与测量引擎切换同批）
- **测量引擎切换**：`@chenglou/pretext` 上游包（选型见待定 #8）——块高真测量替换走查弹的估算+实测反馈环；`prepare()` 缓存纪律沿用 `pretext-cache.ts` 既有模式；内部 `lib/pretext` 随之退役
- **宿主侧块转译 v1**：现有 chat-stream（markdown + 工具事件）→ 语义块——**agent 层零改动**（R1-3 拍板的 staging；走查弹已落地 `paper/translate.ts`，围栏拆分含 diff）
- 画布引擎：**无限画布**平移缩放（D-R1-1 废除「有界」）+ **流锚**（D-R1-3：流向上生长、锚点视口下缘、输入条固定底部）+ 方位感（原点标记/小地图/回原点快捷键——设计义务非结构负担）+ 视口虚拟化（数据全量、渲染窗口化——`measureLineStats` 免字符串统计正好是窗口化测量原语）
- 横轴三条消极决定（D-R2-4）：不预设列结构 / 流锚独占窄带 / 世界坐标唯一真相（x/y 数据模型说了算，纸壳不保留摆放特权）
- 输入条 IME 安全性 spike（§6 风险项前置验证，学 WO-S0A 的做法：先证假设再施工）
- 落 `src-ui/src/paper/`，零 UI 依赖 + 无头测试（对齐 `agent/` 运行时纪律）
- 门禁：build + vitest + biome（不触 agent 面，无 convergence 义务）

### V3b — 壳装配（gate 于组合层 S1——串行拓扑下阶段 1 已完成，自然满足）

- 纸壳视图经组合层挂载（roster 行）；**块渲染器 = ctx service**——组合层第五贡献通道（D0 已预留此接口形状）
- 共居模式切换（先例：`v4-pro-minimal-ab-test-plan.md`）
- 走查弹验收 = R1-2 定义的「这就是纸」时刻跑通

### V4 — 视觉打磨环（短环 × N；**防发散协议**——本计划对「怎么都改不好」的结构性回答）

「Agent 前端怎么都改不好」的病理：agent 每轮重新发明（搜索空间不固定）× 用户每轮给混合反馈（信号不正交）= 随机游走。观测台当年靠很多轮随机搜索 + 人工评分磨到位——有效但极贵。V4 的任务是把随机搜索改造成**梯度下降**，五条规则：

1. **一环一维**：每环恰好一个视觉维度（间距节奏 / 字重层级 / 色彩浓度 / 对齐网格 / 投影深度 / 动效曲线 / 密度 / 留白）。反馈只谈本维，别维的发现记账本，不动手。
2. **对照式反馈（选择优于描述）**：agent 交付的永远是并排 A|B 截图（现状 vs 候选），用户选一个或说"都不对，方向 X"。**禁止"让它更好一点"这种无锚反馈**——那是随机游走的入口。
3. **品味账本**（`taste-ledger.md`，本目录）：每个决定一行——维度 / 上下文 / 选了什么 / 弃了什么 / 一句话为什么。作用：① 已判死的决定不复活（翻案需显式理由）② 新 agent 会话先读账本——**打磨知识不再随会话蒸发**。已付费的部分知识已在三处：`tokens.css`（观测台）、`paper-prototype*.html`、`ink-brass-*.html`——V2 时合并入账（实证：`--obs-brass: #c9a25c` 与白纸原型 `--brass-hi` **完全同值**，动效曲线同源，Fraunces 同款——品味 DNA 本就连续）。
4. **机械 gate 前置**：能用机器查的不过人眼——token 符合（禁 magic number）、间距只取刻度倍数、对比度阈值、字体栈符合。V2 后写成检查脚本进 CI。预计砍掉一半历史上的"不满意"——那半其实是契约违规，不是审美问题。
5. **止损规则（反"永不收敛"本体）**：每维最多 3 环；3 环内 A/B 都不过 → **不是执行问题，是契约问题** → 升级回 V2 改契约（该维 token/golden 错了），不继续磨实现。「更好但说不出哪维变好了」→ revert。V4 总环数预算开工时拍（建议 ≤20）；预算尽 → 接受现状进 V5——纸感来自材质与结构，不来自第 40 轮微调。

**截图回路（已验证可行）**：`src-tauri/src/cdp.rs` 有 cdp_screenshot，`tauri.conf.json` 已开 `--remote-debugging-port=9222`——agent 侧「渲染 → 截图 → read_image 对照金样」的机械回路存在，V4 开工首日钉一根验证刺（真跑通一次，学 WO-S0A 纪律）。

### V5 — 壳切换

roster 级切换 → AB 共居期 → 观测台退役判据（R3 定）。星图展示层退役决策（设计文档 §5.1）在此兑现。

## 待定清单（2026-08-20 盘点；开工前全部消化）

**已定，不必再纠结**：方法论（转录不发明 + 共同创作循环为常态）/ 管线结构 V0-V5 / 块生产者 staging（宿主转译先行，agent 层零改动）/ 独立工程定位（2026-08-20 二次修订）/ V4 防发散协议 / 品味 DNA 三源已定位（观测台 tokens.css + 两套原型）。

| # | 待定项 | 谁定 | 何时 |
|---|---|---|---|
| ~~1~~ | ~~R1 四问~~ **已完成**（2026-08-20：D-R1-1…5 + 走查弹定义确认） | — | — |
| ~~2~~ | ~~视觉方向第一轮：白纸 vs 深色 vs 墨黄铜~~ **已定（R1 D-R1-5）：不选边，主题化**——白纸/深色 = 两主题；墨黄铜并入材质词汇表 | ~~用户~~ 已拍板 | ~~V1~~ |
| 3 | ~~R2 交互细节~~ **已完成**（2026-08-20 R2：拖出钉住+动手权 / 按钮确认收回 / 内推外拉+活引用 / 横向开放主权区，见 interviews/R2-2026-08-20.md） | — | — |
| 9 | **工作区概念转向（engine 中心 → Agent 中心）**——**方向已定（2026-08-20 用户拍板，ADR Accepted）**：软件用户面 = 纯粹的 Agent 软件，图谱无常驻 UI 位（内化为后端支柱 + 以块形态作为对话中的物出现）；Workspace 翻转为 Agent 的家，图谱降为会话属性。剩余仅粒度子题（跨目录会话/绑定粒度/无图谱一等入口/共居过渡）。见 `docs/adr/workspace-concept-ownership.md` | 子题：用户（走查弹后）；施工：agent（S2 完成后，与 S3 同窗） | 方向已锁；子题走查弹后 |
| 4 | R3 退役：共居期长度、观测台退役判据、星图退役兑现 | 用户 | V5 前 |
| 5 | V2 契约内容：三源 token 合并 + 金样清单 + 组件清单 | agent 转录，用户验收 | V1 定向后 |
| 6 | V4 止损参数：每维 3 环上限已定；总环数预算（建议 ≤20） | 用户拍板 | V4 开工时 |
| 7 | ~~截图回路验证刺（cdp_screenshot 真跑通一次）~~ **✅ 已验（2026-08-21 提前钉刺，早于计划的 V4 开工首日）**：回路四段中三段实测打通——① 应用自身 webview 经 `tauri.conf.json` `--remote-debugging-port=9222` 应答 CDP（`/json/list` 可见 `tauri.localhost` page target，标题「全息观测站」）；② WS `Page.captureScreenshot` → PNG 落盘成功（1000×700，与窗口配置一致，591KB 非空）；③ PNG 结构合法（签名/IHDR 校验过）。**第四段 read_image 因当前会话模型（GLM-5.3-think-max）不支持图片输入未在本会话验证**——留给后续 vision 会话补一根刺即可，属模型能力问题非回路缺陷。spike 产物已清理。 | agent | 已完成 |
| 8 | ~~IME spike、文本块排版纪律、虚拟化方案~~ **测量引擎选型已定（2026-08-21 用户发现上游）**：切 `@chenglou/pretext`（npm 正式包，49.9k★，MIT，2026-03 起活跃维护）——与仓内 `src-ui/src/lib/pretext` 同源同思想（都出 Sebastian Markbage 的 text-layout 研究线；内部版是其冻结快照），能力超集：手动逐行布局（`layoutNextLineRange`/`walkLineRanges`）、变宽流式、rich-inline、免字符串统计（虚拟化正好要用）。**内部版 V3a 时退役**（`pretext-cache.ts` 随观测台聊天虚拟化继续服役至退役）。已知边界：canvas font 简写外 CSS 特性不建模、`system-ui` 在 macOS 不准（有 PLATFORM_BUGS.md ledger）——纸壳字体栈全具名（Fraunces/Noto Serif SC/JetBrains Mono），正好避开。走查弹里的估算+实测反馈环是其过渡替身，V3a 换正式测量后拆。IME spike、文本块排版纪律、虚拟化方案仍归本项 | agent | 选型已定；施工 V3a |
| 10 | **块粒度与截取**（走查弹涌现，2026-08-21）：长文字回复整块拖出没必要——用户想「只截取一段文字」钉出。设计空间：段落级拆块（转译层拆 text part）/ 选区拖出（选中文字拖离流）/ 混合（块=默认、选区=精细）。牵连：块 id 稳定性（转译层续命机制）、「块=活引用」语义在子块粒度上的形态、D-R2-4 世界坐标归属。**先出并排原型对比（V3a 前的一小时级探索），人判后进 V3a 数据模型** | 用户（选型）+ agent（原型） | V3a 开工前 |

用户在本工程的全部出场：#1 访谈（一轮）、#2/#3 选择与访谈、#4 一次拍板、#6 一个数字、**V4 起进入长期共同创作模式（高频沟通-修改-测试循环，这是工程本体，直到你满意为止，没有预设上限）**。其余全部 agent + gate 承担。

## §6 问题分配表

| 设计文档 §6 待决项 | 归属 |
|---|---|
| ~~白纸 vs 深色~~ | **已拆题（D-R1-5 主题化，不选边）**；余下主题双套机制 → V2 契约 |
| 写字段落格线 | V1（原型参数化已完成，变体即可） |
| ~~横向维度角色~~ | **已定（D-R2-4 开放主权区 + 三条消极决定）**；走查弹只验「平移过去是一片可用空」 |
| 文本块排版纪律 | V2 契约 + V3a 布局数学 |
| 中文输入法兼容 | V3a IME spike（前置） |
| ~~钉住/引用最小交互~~ | **访谈已毕（D-R2-1/2/3：拖出钉住/按钮确认收回/内推外拉）**；实现 → 走查弹 + V3b |
| 长会话虚拟化 | V3a（Pretext + 窗口化渲染） |

## 与组合层计划的排程（2026-08-20 拍板：三阶段串行，用户带宽优先）

- **阶段 1 = 组合层核心**（`../composition-architecture/` S0→S1→S2）：白纸不动工。用户角色 = 批 S1 设计件 + 绿灯放行，零深度参与。
- **阶段 2 = 白纸全程**（V0→V5，本计划主体）：agent 全部产能投前端；用户注意力集中于此——但均为短会话模式（访谈 / 选型 / 打磨判读），不是巨理解模式。V3b 的组合层 gate（S1）届时早已满足（串行拓扑下自然解）。
- **阶段 3 = 组合层收尾**（组合计划 S3→S4）：S3 面板迁移此时才有纸方向可依——先迁再定方向 = 给可能被纸废除的面做返工（组合计划 S3 协作纪律的必然推论）；纸壳的面板/命令贡献走组合层通道，不自建旁路。
- 例外（可选，零义务）：阶段 1 期间随时可做 V0 访谈——那是纯对话，不产生任何工程排程，做了只是让阶段 2 起步更快。

## 明确不做

- 不在原型阶段写生产代码；不让 agent 在生产代码里发明视觉（只许转录契约）
- 不做模型原生块输出（宿主侧转译先行；若证据显示需要模型原生块，那是 agent 层工程，**独立立项**走变更流程）
- 不在本计划内动 agent 运行时 / 冻结文件 / convergence 体系
