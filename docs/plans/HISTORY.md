# docs/plans — 里程碑时间轴（施工史）

> 本文件是 changelog，不是现状。**现状入口是 [`README.md`](README.md)**——想看「现在在哪/还剩什么」读那一页。
> 收录规则：跨工程里程碑级事件（拍板/竣工/翻案）；单项工程内部批次记录在各计划目录与 taste-ledger。
> 倒序 = 最新在上。

| 日期 | 里程碑 | 工程 |
|---|---|---|
| 2026-09-24 | **引线纯指示化（两条腿）+ 归因更正**——用户先问「为什么我的创作坞在选中一个会话卷的时候，这个引线是可以选中的」，再追问「嗯？会话分支的引线也是可选中的？」，并否认旧账：「我从来也没有拍板过引线本体要做成按钮」。两条腿原都被做成**控件**（受墨带 + `role` + `tabIndex` + 点线飞纸脚/飞父卷节点 + hover/聚焦抬墨），账上却记「同日拍板 可点溯源」——判据：用户原话是疑问句、待验收清单从未列「可点」、存档 168 条用户消息零命中（同 09-21 界栏线，自纠留痕）。治法：两腿受墨带/点击/焦点态/抬墨两档摘除 + **受墨带 CSS 族整族删除** + 两层 `aria-hidden`；测试面 7→6、6→5 例（两条「点线溯源」例同批删除）。**代价**：「跳回父卷分叉节点」入口随之消失（卷首「枝」缀字是纯文本），恢复须另立真控件 | paper-shell |
| 2026-09-24 | **机读位复原：机器文本换真等宽（用户拍板 B，翻案 09-10「非等宽对齐」代价）**——① 真 bug：UA 对 `<code>` 有直接规则 `font-family: monospace`，直接规则压继承 ⇒ `<pre>` 上声明的 `var(--f-mono)` 从未生效（纸面代码/引文/命令输出一直在用浏览器默认等宽，中文落宋体），治法 = `foundation.css` 全局 `pre code { font: inherit }`；② 机读位换 `Noto Sans Mono CJK SC`（OFL 1.1、随包自托管、中文也等宽），`--f-mono` 与 `FONT_STACKS.mono` 逐字同值加守卫；③ 同批前序：围栏回归独立**抄录**块（`1ec01b65`） | paper-shell / 渲染 |
| 2026-09-23 | **模型配置面三层重构 + 思考档位下沉（用户实测三问，拍板方案 A；`c89f92b6` + `117c35bb`）**——① 一个 `models` 数组身兼目录/启用/选中 ⇒ 拆家：新增 `ProviderSettings.catalog`（拉取快照），「从 API 拉取」更名**刷新目录**且不再灌满可用模型（Provider 页目录区勾选才进 `models`）；② 拆掉创作坞下拉的 `.slice(0, 30)`（用户报「拉了 81 个，下拉里没那么多」）；③ 思考档位下沉 `ModelOverrides.thinking` + `modelThinking` 尺子，行级值退化为「默认思考档位」，创作坞 pill 换源 provider 作用域描述符、会话不再快照行值。宿主面 +3 键（产物热更需重建 exe）。见 [`provider-system-spec.md`](../design/provider-system-spec.md) | provider / 画布支 |
| 2026-09-22 | **立枝切点按块修正（用户报「复制了一整个会话」）**——入口挂在**块**上，定位只认「消息」；实时形态一条助手消息**聚合整轮**（流式助手跨步复用同一条），点中间任意一块都切到整轮末尾——单轮卷上 = 整卷。真机取证：卷 27（29 事件、一条来文、一轮 8 次调用）的枝卷 29 头行 `parent {id:27, atSeq:29}` = 父卷最后一个 seq。修 = **块坐标**（parts + 本块下标）进定位链：邻块顶替 → 工具卡按 `tool_call_id` 身份 / 正文按本轮内容+块序 → 吃掉紧邻的工具结果；只给消息的调用方仍走整轮末尾。粒度边界：正文与同一条消息里宣布的调用不可切在中间 ⇒ 切在该步结果落定之后；未落定照旧具名拒绝。零 BCR（未动事件词表/投影）。见 [`session-tree-plan.md`](session-tree-plan.md) §12.13 | session / paper-shell |
| 2026-09-22 | **landmine L5 状态格补记：后台活体不计入「在跑」**——裁定本身是**用户 2026-09-20 拍的**（留痕在 `tests/async-return-delivery.test.ts` 头注，commit `7015fac9`），但 landmine 状态格当时漏记、连挂两天 ⏳ 待拍板；2026-09-22 用户复述同一裁定（「本卷 Agent 就是空闲的，可以继续和用户交互，没问题」）后补记，两条互证。⇒ 派发点**不**寄 `kind:'subagent'` / `kind:'bg'`，五个读面（呼吸线 / 停钮 / 侧栏状态点 / 行卷中 / 退出守卫）一律不改：「Agent 忙不忙」与「还有什么在跑」是**两个信号两个面**，后者由同日落地的**役册**承担。**接受的代价**：在役后台任务不升起退出确认，关窗即被 lifecycle 终止（判据注释同批改写）。**教训**：拍板落在测试头注里、没回写状态格 = 两天文档漂移 | agent / 文档面 |
| 2026-09-22 | **役册落地——后台工作监视面（创作坞落位）**——用户报「每个会话跑后台任务，我没有一个能监控的地方……往创作坞放，还是往标题栏放，还是直接放会话流区」。**裁定**：标题栏**排除**（用户已于 2026-09-17 自己拆掉，§14——往那儿长＝重建刚拆的，并付边缘滚动与窗口钮两笔代价）；流区**排除**（块是消息的派生视图、要落盘有块序，`EventKind.Notice` 的 info 级没有入流通路；流区已有正确位置 `SubAgentPart`，那是记录不是监控台）；**创作坞落位**（唯一同时满足常显 + 跟活跃会话 + 有同类先例——§9.1 墨量册已为「读数件 + 坞内向上开的浮层 + 并入互斥」立法）。**数据面**：Agent 侧三本账都读不出「按会话的在役清单」⇒ UI 侧自建役台账（子 Agent 走 `AgentUINotifier`，shell 走 `background_activity` 对账 + `bg:note`）。只读纪律：绝不调 `bash_output`（共享游标 + 终态即删账）；终态不编造。行序**显式规格变更**：行尾扩为「役 \| 墨」。**须重建 exe** | 画布支 |
| 2026-09-22 | **匣脚引线落地（`b5f12426`）**——多会话卷的激活态：创作坞顶左端那枚**版口钮**（匣口）连到**活卷纸脚**（版心左缘 × 卷底边，纸的材料缘）一条常显朱丝，可点溯源（飞到线的另一头 = 纸脚）；切卷线改指、无活卷不画、活卷出屏线照样出屏。卷端落点一版接天头版口钮（「红对红」）被用户当场打回「你这个引线能挂在页尾吗……你挂在第一条用户输入那不是乱了套了」⇒ 二版改纸脚（天头那枚是「哪一卷活跃」的标记，不是「这一卷在哪儿」的位置标记）。引线笔补全判例两处缺项：**锚面外法向**（按锚面定，不按相对方位现算）与**臂长下限**（取值由台架读数定：下限 24 时净抬起只剩 2.6px），缺省值恒等于既有两条腿的逐字节行为 | paper-shell |
| 2026-09-22 | **纸壳引线三刀 + 锚点批**（`04557a65`）——切向贝塞尔换掉直弦、每块两缘固定锚点（hover 显形）；侧栏删除武装期「点别处 = 取消」补成真手势（`edee40b8`）；书脊列书口档改**默认形态**（用户实机看过后拍板，`2108cf24`） | paper-shell / 画布支 |
| 2026-09-21 | **卷号治理 A+B 落地（用户拍板 A+B，`d3f312ad`）**——号保留为身份但**永不复用**（每工作区发号账 `_issue.json`：发出即记账、重启不回退）+ 有名卷常显号收成两处（书脊档号 / 卷首眉行）；C「换 UUID」不做；顺带拆立枝发号的 await 前快照竞态。同日书脊列溢出治理（书口档：写题名不写编号 + 滚轮热区修复，`dda062f2`）与设计感复原（脊块归位纸片件 + 整数行几何，`4663955a`）；目次带界栏线摘除 + 归因更正（`d14dc380`——「用户拍板」系记录失实，用户原话「我从来也没拍板过」，自纠留痕） | session / 画布支 |
| 2026-09-20 | **契约 v42 → v43 → v44 三连升版 + AgentConfig 死字段清理（28 → 23）**——① 斜杠命令面重做：唯一真源收归 `ctx.commands` 通道，`/` 面板与 Ctrl+K 消费同一份合流清单，旧 `command-registry.ts` 整文件退役（`725b0661`）；② 运行记录表取代「声明式」运行态（`d9c1b431`，`54981624` 补两交账点）；③ `AgentLoopHost` 三成员 + run/abandoned 事件面（`81f1606b`，零漂移）；④ `skillRegistry`/`taskManager`/`messageBus`/`taskBoard`/`discoveryBoard` 五字段退役（`5c3c26c8`） | agent / 组合层 |
| 2026-09-20 | **案卷侧栏双视角 P1-P5 落地（用户拍板「丙 · 双视角」，`7d50ac6b`）**——案卷视图 = 纯时间序扁平列表 + 「枝 N」血缘牌（父卷号并在牌内，`↳N` 记号当日退役）；枝视图 = 森林（族不拆、引线折角、`▾ N 枝` 折枝、hover 整族高亮）；走查与本机几何对拍见 `prototype/sidebar-tree-ab.NOTES.md`（该目录在 .gitignore 内，证据不随仓库分发） | 画布支 |
| 2026-09-20 | **插件产物热更根治（landmine H4/H1，`f95b61e2`）**——JS 入口 URL 带版本号（换产物即换 URL）+ 自动镜像与自动重载；断流可见化 + 在途看门狗（`fc8971b2`，H6）。同日侧栏行操作跳变根治（`d33e6daf`——显隐/武装/提示一律不改占位，七处实测位移归零）、侧栏「枝」牌只留「枝」（`9863ad06`） | 插件 / 画布支 |
| 2026-09-19 | **会话树（枝）全批落地 + 卷日志抹除 A 案**（[`session-tree-plan.md`](session-tree-plan.md) / [`session-log-erasure-plan.md`](session-log-erasure-plan.md)）——立枝双入口（消息动作行「立枝」+ **空间手势**：块上「枝」握把拖出引线、松手落在纸上就地立枝）；侧栏树形 + 书脊/卷首「枝」标 + 未落定置灰；删父卷**连坐整棵子树**；画布朱砂引线连回父卷分叉节点、点线溯源。抹除：改/重发后旧内容**从 `.ndjson` 物理抹除**（撤回即压实：整份原子重写 + 头行 `erased` 账声明空洞；投影/词表/载荷零改动 ⇒ 零 BCR）。**旧枝考古不做**（用户裁定 §9） | session |
| 2026-09-19 | **记忆墓碑语义退役 + 夹注↔脚注叠字根治**——删掉的记忆不再靠墓碑存活（`7fb72ed5`：索引写链 + 读侧记忆判据）；夹注进实测回写族 + 实测记录带渲染态签名 + 行高按 CSS 用值量化（`e2543571`）。同日分支子卷卷名乱套根治（`4218ed7a`）、LOD 墨迹几何改由测高走查产出（`12dd9cce`）、合卷落盘补齐 token 账本（`fb63c94d`） | memory / paper-shell |
| 2026-09-18 | **引擎工具面收敛：36 个扁平工具折叠为「域 + action」（契约 v4 → v5 → v6）**——域折叠 + help 取全文 + hint 补前置纪律 + 删退役开关（`e4203bbf` / `8415599e`）；契约面指纹 guard 落地（「自称有 guard」变成真有，`d1346049`） | 引擎 |
| 2026-09-18 | **出处引导（pin-provenance）代码落地 + 卷首字节稳定性立项**（[`pin-provenance-plan.md`](pin-provenance-plan.md) / [`prompt-prefix-stability-plan.md`](prompt-prefix-stability-plan.md)）——钉块页边注第三行常显「摘自 卷名」（不依赖视口内目标）+ hover 引线拉到源洞（**洞离屏也出屏**）+ 点行溯源飞到洞；连带修飞行动画混钟（jsdom 实测循环永不终止、视口飞出 16 万 px）。字节稳定性：记忆库坐卷首第 ~2.5k token ⇒ 每次「重启 + 记忆变动」整卷重算（实测命中率 99% → 0.6%），「把记忆段挪到卷首末尾」已验算**无效**（只省 2.8%），含纠错留痕 | paper-shell / 记忆 |
| 2026-09-18 | **provider 连接怪癖批竣工合入（`e5f9e27e`，worktree 合入）**——自定义请求头 + 配方导入导出。同日引线重做（从「连线图」回到一支笔，划词朱线同族转录，`edd1aa26`）、装配输入收敛单一真源（手抄表退役，漏搬结构性不可能，`781078b9`)、运行态单一权威源（装配账本与句柄同源，`b67ac7e8`）、案卷列表载入成本根治（刷新分频 + 卷清单投影缓存，`afa1277e`） | provider / paper-shell / agent |
| 2026-09-17 | **测试基建换轨：默认测试环境 jsdom → node**（`19773700`，82 文件；81 个真碰 DOM 的文件自带 jsdom pragma）——测试墙钟大幅下降。同日拆除 browser screenshot 的 inline data-URL 上下文炸弹（P0b，`a3c88617`，BCR 已批准放行，`4cdab957` 提交申请） | 测试基建 / agent |
| 2026-09-17 | **工具附图通道 P0a 落地（agent 眼睛环）**（[`tool-image-context-plan.md`](tool-image-context-plan.md)）——工具产出的截图进模型可见通道（此前只挂 user 消息 ⇒ 模型看不见自己的产出，靠用户眼睛逐轮喂）；P0b（元素级截图 + 描述改写 + 拆 `inline`）需 BCR 放行。同日科研渲染题签铺满（board/timeline/grid/图片题签行，`789043fd` 等） | agent / 渲染 |
| 2026-09-16 | **文档面大重构 P0-P3 落地 + P4 在推**（[`doc-surface-refactor-plan.md`](doc-surface-refactor-plan.md)）——确立「读者 × 权威 × 生命周期」四层形态（L0 注入层 / L1 规则层 / L2 现状层 / L3 生成层 / L4 过程层）+ 事实与体量门禁 `npm run doc-check`（真源 `scripts/doc-facts.cjs`），**跨文档标量只准来自生成物或写指针**；P3 归档大扫除（11 件竣工件归档 + P3b 补批 11 件）使 plans/ 只剩在办项 | 文档面 |
| 2026-09-16 | **组合层 S6 全段竣工并归档（S0-S7 全树）**——插件激活账地基：**登记 ≠ 激活**（引用计数 + `manifest.activation`，契约 v38，`e508f093`）+ 受治进程接激活账（lazy 档「引用归零即停」，`0eab879e`）；组合依赖与独占装配期判定 `requires`/`exclusive` + 诊断第四栏（契约 v39，`2259c3c3`）；程序入口落地（只收 preset id，不可解析则拒绝创建）（`9f330b82`）；性能对表与 profile 断言 P3c 收官（`aa5b491d`）。全文 [`../archive/composition-architecture/`](../archive/composition-architecture/README.md) | 组合层 |
| 2026-09-16 | **3D 视图整量拆除 + 卷首版心天头（用户拍板 B 案）**——dsh-bundle 只发引擎 + MCP 工具面（原先随包的 3D 星图随图谱渲染内核退役拆除）+ 引擎接线服务名修复 + release-bin 引用清理（`76b47894`）；卷首四行（玉徽/眉行/题字/档行）**同轴居中于 720 版心**，此后不得回到「纸缘内距左齐」（`327a5db6`） | dsh-bundle / paper-shell |
| 2026-09-15 | **会话存盘换轨 Phase 1-3b（DSH 参照）——权威翻转**（[`session-persistence-dsh-port-plan.md`](session-persistence-dsh-port-plan.md)）——① Phase 1 事件日志 append-only 写面（`164a884c`）；② Phase 2 崩溃恢复链（断尾截断 + 悬空工具调用配平，`1c6aa0b1`）；③ **Phase 3b `.ndjson` 即卷本体、`.json` 降级为投影缓存**（`1a200bad`）+ 采用原语（adopt：日志即历史，开卷不写全文副本，`a224fcae`）；④ 触发点 B：工具副作用前的持久化屏障（`fe52e6d1` / `2def2f25`，phase-5 基线变更已批准） | session |
| 2026-09-15 | **组合层 per-agent 组合 S6 立项 + P1 收官**（`7575c247` / `a87b620a`）——选择集语义 `defaultOff` + 诊断按原因分栏（开放面契约 v31，`6abbcc30`）；P1d 会话工厂判据换轨（组合身份比较取代对象引用，消掉每卷白建注册表，`cca04a58`） | 组合层 |
| 2026-09-14 | **app-region 全面退役 + 目次带 v3 立法**——标题栏触发范围收归应用自管（命中范围由元素自管 + 壳层兜底，用户三报直指根因，`6d9742cc`）；目次带 v3 立法（规格书 §13）+ 乙案落地（识别层换装：形状 + 墨量 + 族色，用户拍板 F，`1178ccde` / `44deb894`）。同日插件化收口 M1/M2/M3a/M5（贡献通道单层化 + 通道/实现分家 + 硬编码隐藏清单清零，`271f72ae`） | paper-shell / 插件 |
| 2026-09-13 | **OfficeCLI 改判一等 `office` 域工具（C 路）**——MCP 挂接路退役，单二进制 Office 套件接成 `office(action,…)` 域工具（`2d54081f`）→ [`office-cli-integration-plan.md`](office-cli-integration-plan.md) | office |
| 2026-09-11 | **Provider 模型元数据拉取落地 + 附图转发断点根治**——窗口/视觉/推理能力从 API 拉取、持久化、抵达 wire（`08f379cb`）；附图转发断点根因 = AgentRuntime 壳吞掉 run 第三参（真机贴图不进会话，`2a071a0b`） | provider / agent |
| 2026-09-10 | **全产品换装 MiSans（三体换代拍板）+ 自动选中整体退役**——宋/楷/等宽三体退役，单一可变字体（`38af8917`）；自动选中（浏览跟随）随拍板**整体退役**——活跃会话转移改显式动作独占（`a6adaa8d`，连带勾销画布 Stage-4 的 P1-1 待验项）。同日 ask_user 用户侧六面根治（`7a9a51c7`） | UI / 画布支 |
| 2026-09-09 | **图谱功能全量退役——兰台零引擎内置接线，引擎回归纯 MCP**（`51047f99`，强制层改动 + 宪法审查）——应用内图谱工具面整量退役，引擎降为随包配套的独立进程（默认关）；baseline 重录（standard + minimal 双 preset，`f1ca3dd8`）。同日 lspd 连接被 RST 根治（`5e9dd5b2`）、消息消失/聊天流冲坏根治（`4a6f3b66`）、首页点不进工作区根治（`30fd7bd8`） | 图谱 / 壳 |
| 2026-09-09 | **HoloGram → 兰台全量改名收官**（`8f190a8e`）——文档与配置改名一次性收口（应用 = 兰台 / Lantai；HoloGram 保留为图谱引擎专名：工具域 `hologram(...)`、`.lantai/`、`HOLOGRAM_*` env 不改）。同日 multimodal-image B4+B5 竣工落账（五批收口，余真机六项） | 命名 / 渲染 |
| 2026-09-08 | **引擎-宿主逻辑全断竣工**（[`engine-host-severance-plan.md`](engine-host-severance-plan.md)）——壳摘除 hologram-graph/storage/vector **全部 crate 依赖**（`2cd4d751`），壳对引擎的知识收敛为「spawn 二进制 + MCP 协议」两条（`495b6652` 契约与文档去宿主化）；引擎资产归位 `engine/`（onnxruntime/models/grammars 随引擎走，`8f6b3439`）。同日 OAuth 订阅登录前置 + 模型真实拉取（`6575c04c`）、上下文压缩迭代对齐 DSH（触发 0.8 + 尾部预算，`d98cd27f`） | 壳 / 引擎 |
| 2026-09-07 | **科研渲染 4A/4B 双通道落地 + Skills 生产级改造 + 用户级 MCP**——科研渲染五 kind 落地（math / citation / chem / chart 交互 / grid 虚拟滚动；4A/4B 用户实机验收通过）+ §5 轮子策略定稿（换 vs 加 vs 补）；Skills 生产级改造 Commit 1+2（yaml 解析内核 + 用户级目录 + 装配期发现注入，`4667f2b8`）+ 技能管理 UI（`e102659b`）；用户级 `~/.lantai/mcp.json`（跨项目个人 MCP server，`00f8cd4b`）+ 设置面板 MCP tab（`a9d82b2a`）；Provider 方案乙三期（协议开放 + Responses + Codex OAuth 订阅，`63f488f9`） | 渲染 / Skills / provider |
| 2026-09-06 | **app-shell 软件级插件 S1-S6 落地 + PaperPanel 拆解 + 宪法增补条款 5**——软件级插件四件套：S1 数据目录（`dataDir` + 五命令 + 卸载 `.trash` 回收）/ S2 受治进程治理（ServerGovernor 三档生命周期 + 进程树终止）/ S3 窗口原语（窗口注册表 + iframe 视口 + postMessage 白名单桥）/ S4 后台唤醒（deferred 包装）+ S5 范本 `examples/plugins/notes-app/`。PaperPanel 2692 行巨件按域拆 16 个 hook（装配根瘦身至 ~470 行，纯行为保持，`8dc41013`）。**宪法增补条款 5「测试改造禁令」**（重构下测试只有三命运：零改动 / 整删 / 新写，`7bc6a825`）。同日内置插件名册单一真源 `builtin-roster.json` | 插件 / paper-shell / 治理 |
| 2026-09-05 | **内核能力口收口 R5 竣工（拆除令全文兑现）**——十一插件全退役、换轨 `*_cap` 能力口直呼：fs（`4cfe3adf` / `a681adc7`）· git（`f3add174` / `c5acb876`）· shell `process_cap`（`5edb9c28` / `86d0d659`）· browser（`e8e9fb6d` / `b5540255`）· desktop `uia_cap`（`506dc7e3`）· editor（`f6b74343`）· search（`5e563923`）+ web/constraints/pty/lsp 四族立口换轨（`a3eec0ee`）；R5 主体拆除（tool_call / PluginRegistry Rust 脚手架收官，`6819d63e`）。决策真源 [`kernel-plugin-architecture-decision.md`](kernel-plugin-architecture-decision.md) v3：**TS 策略建议层 + Rust 能力口强制层** | 内核 / session |
| 2026-09-04 | **内核插件运行时 Phase 0+1+2 全量竣工 + 钉住与纸条换装 + 拆壳转 C 模型**——Phase 0+1：ToolPlugin 契约/注册表/`tool_call` 统一入口 + `builtin.search` 首迁（`4c191466`）、`builtin.web`（`4778cd5f`）；Phase 2：七域信封化（fs `fcd120b4` / constraints+editor `87ab054a` / shell `b74d18c6` / git `e2593712` / browser+uia `c12281e8` / pty+lsp `d419c9d7`）。拆壳 D0-D2（原语后端骨架 `bd37e286` / client `50e6d904` / fs 字节执行外置 `94152c58`）**当日转 C 模型**（退役 primitives-server，字节执行回 exe 能力口，`712fda8d`）。钉住与纸条换装（松手定夺 + 杀路径 B + 盖显形，`0dfc4979`） | 内核 / paper-shell / 插件 |
| 2026-09-03 | **插件 bundle 退役 S2-S6 竣工（内核表瘦身：出厂产物改走磁盘产物通道）**——S2 六供应商真源产物化（`faabce58`）、S3 十六工具域 + 两段贡献薄壳迁各自产物目录（`a39fe92e`）、S4 装载调度层（依赖图 PENDING 挂起 + boot 全 ACTIVE 审计 + fail-loud，`c15c3849`）、S5 bundle 双轨拆除（`79c9a400`）+ S5b agent-loop-service 产物化（`7fae27f1`）、S6 文档同步（`48c2dafd`）；**保险丝两根**（face 键集对拍门禁 + 渲染面错误边界，生产事故立法，`c1e4acc6`）；装载串行改三波并行（墙钟 2.0s → ~0.17s，`6f90923f`）。同日 stream-rhythm 五刀全落地（工作单元 + 族节奏切单元 + 目次带阶段导航） | 插件 / paper-shell |
| 2026-09-02 | **AURA SDK 语义记忆系统拆除 + 画布状态分片存储 + 性能批**——AURA SDK 语义记忆整系统拆除（`c7866c32`）；画布状态分片存储 v2 + 公共物分片 + v1 迁移（`f2dcca08`）；性能三刀：卷级虚拟化（视口外流区跳过全量派生，`28018d5c`）、画布平移零重算（卷级布局核心缓存 + 依赖收窄，`20447629`）、启动并行化（装配与 RPC 并行 + `workspace_list` 缓存 + 快照门闩，`3fffc3a8`）。同日目次带 v2 minimap 换血（内容指纹 + VSCode 交互语义 + 语义刻痕/活线/未读，`e01b24a3`）、跨工作区串卷根治（`99f31f66`） | 记忆 / 画布 / 性能 |
| 2026-09-01 | **浸墨法则入宪 + §11 层次法立法（三轴模型）+ 纸面真纸化 + RPC 校验层批一**——浸墨法则入宪并五面转录（首页/画布/创作坞/侧栏，治「简陋」，2026-08-31 用户拍板 B，`2fb19035`）；§11 层次法立法（**海拔 / 包含 / 轻重**三轴 + 四档海拔梯子 + 选中语言分型 + 四问清单，`230c8456`）+ 三轴建设批施工（字重四级收编 `e99229a1`、z 散写收编 token `98bdf2be`、泛框泄漏案废案 `2a4710a8`）；纸面真纸化（feTurbulence 程序噪声换真纸纹理资产 + 方向光，`db441fa9`）+ 流区真纸每卷一张 + 纸性哈希（`b2f53c4a`）；`typedJsonRpc` 边界运行时校验层批一竣工（`rpcResultSchemas` 同址共治 + 签名一刀切收紧，`ea1606b6`）。同日修平移 `setView` 未按 rAF 帧合并致 React #185 更新嵌套爆限（实机「流区透明」根因，`0fdf4787`） | UI / RPC / paper-shell |
| 2026-08-31 | **kind='feature' 全量通道化竣工 + 第一方代码热更 P0+P1 + 创作坞 v2**——位移式内置插件装载 + UI 四面源码产物化（`e9f2b05f`）——出厂态零特权行收口；第一方「改完即生效」P0+P1 全量落地（`dev.cmd` 热更工作流 + 渲染器插件通道化，`6b5ee6eb`）；创作坞 v2 案头批（空态案头/880 版心/引·拖放入卷/律册/墨量线/钤印拟文/翰/运行呼吸线，`867ad863`）；案卷侧边栏注疏重排（分节/检索/键盘导航 + 术语收编「会话 → 案卷」，`94eb5fe4`）；纸感浸墨化（熟纸纸阶 + alpha 墨水 + 双频纸纹 + 墨渗缘，`9c5dae4d`） | 插件 / dev / 画布支 |
| 2026-08-30 | **pretext 排版引擎 P1-P5 竣工 + 卷头落地**（[`pretext-typography-plan.md`](pretext-typography-plan.md)）——从高度计算器升级为**纸面排版引擎**：P1 lift 遮罩 / rich-inline 精确测量 / 来文变宽纸条（`550973c6`）、P4 缩远墨迹 LOD 行条骨架 + 小地图真墨（`14fa755c`）、P5 夹注眉批化（夹注吸附正文右侧眉批栏，方案甲，`a1d458bf`）、流式增量渐显（只让新字符淡入，方案 B，`29f44894`）；卷头落地（玉徽亭徽 + folio-head 原型转录，标签带退役，`5032917b`）。同日工具层上下文腰 T-1/T-2（`d1e32320`）、markdown 增量解析（`d0958a59`） | paper-shell / agent |
| 2026-08-29 | **引擎插件化全计划竣工（Phase 0-5 一日连推）**——①契约化（v1→v4：版本 + 载体文件清单 + 壳专属方法清单 + 字节稳定对拍）；②壳专属方法 11 个下沉引擎（hidden tools：`tools/list` 不可见、`tools/call` 可达）；③分页栈整链删除；④壳侧 transport 切换 + **摘 hologram-engine Cargo 依赖**（每工作区一个 `engine serve` 子进程，stdio MCP + 崩溃重启闭环 + 双工作区 e2e）；⑤**免编译扩展面**（`HOLOGRAM_PLUGIN_DIR` manifest 三类：language / framework / tool）；⑥gen-engine-plugin-contract 挂 doc-sync。逐 Phase 落点见 [`../archive/engine-plugin-extraction.md`](../archive/engine-plugin-extraction.md) | 引擎插件化 / 总控 |
| 2026-08-29 | **第一方插件收编插件列表（`49b00587`）**——第一方插件获得清单身份（`plugins/first-party-manifest.ts`：name→version/description/kind）并进设置面板插件 tab，三组陈列：平台服务（service，常驻不可禁）/ 内置插件（feature，可启用禁用）/ 已安装（第三方）。feature 禁用经 `state/plugin-prefs.ts`（localStorage）持久化、**下次启动生效**（第一方是编译期 bundle，不做运行时 fiber 手术）；装载结果统一收 `state/plugin-store.ts`（`mergePlugins` 按 name 合并，外部异步装载不冲刷第一方记录）。计数见 [`../facts.generated.md`](../facts.generated.md) | 平台化 / 总控 |
| 2026-08-27 | **平台化 Phase 2 竣工（后端能力 seam——`4c7b6467` ctx.fs / `4bcd0c05` ctx.shell / `daae88c3` ctx.sessionPersistence+ctx.graph）**——D11 落地四通道：fs 11 动作 / shell 四动作（**D11 修订：ctx.subprocess 并入 ctx.shell**——无第二消费者不开空通道）/ 会话持久化六动词 / 图分析（经 hologram_call 派发）。默认 provider = Rust/engine 恒等包装逐字节一致（P2-C1），fake 替换测试钉住零消费面改动（P2-C2），**P2-C3 强制层不旁路守卫**（plan 激活拦截 fs(write)/shell(run) 时 provider 与 dispatch 双未触）；拆旧清单 T8-T11 清零。全文见 [`../archive/agent-platformization-plan.md`](../archive/agent-platformization-plan.md) | 平台化 / 总控 |
| 2026-08-27 | **平台化 Phase 1 竣工（swappable seam 开放——`98a6f30d` ctx.llm / `605d6e8a` ctx.subagents / 施工③ D4 事件面）**——① ctx.llm：ProvidersService→LlmService，两条第一方 adapter 贡献，裸路径 PROVIDER_DIALECT 响亮降级（P1-C1/C2）；② ctx.subagents：SubagentsService + builtin/in-process 默认 provider，消费单点改查注册表（P1-C3）；③ D4 事件面：5 工具事件扩 13 事件表（turn/step/request + subagent/spawn·done），`Agent.onLoopEvent` 监听面 + 七发射点接线。每段拆旧清单 T1-T7 清零；baseline 零变更。全文见 [`../archive/agent-platformization-plan.md`](../archive/agent-platformization-plan.md) | 平台化 / 总控 |
| 2026-08-26 | **创作坞+提供方联合体检 + 方案甲施工（`efc74e7d` 方案甲 / `254ad008` 全批修复 / `36ad9ed5` 落账）**——两份独立体检互证合并为 [`../plans/canvas-space/composer-provider-audit.md`](../plans/canvas-space/composer-provider-audit.md)，20+ 项毛病一次修完。**方案甲定案**（会话级模型/思考真生效）：全局 = 新卷/未改卷的实时默认；会话 = 只存自己的覆盖——根治「假 per-session」与「thinking 写错行」。修复面：模型下拉列全 / 只列已配置厂商 / 无 Key 标注 / 测试连接最小落盘面 / 历史导航与焦点死链接活 / 后台运行态指示。实机验收七项在册 | 画布支 / provider |
| 2026-08-26 | **画布 Stage-4 会话内体验落地 + 实机返工修复**——创作坞（模型/权限/思考/发送/停止集成）+ 目次带（轮次锚点导航）+ 自动选中三道闸；实机体检暴露 P0-P4 九项（含全放确认+关窗崩溃 0x40000015、自动选中 panningRef 根因、模型选择器/思考档位/权限控件 UX 重做）同日修复，门禁全绿；返工清单待实机复验勾销 | 画布支 |
| 2026-08-25 | **平台化立项 + Phase 0 宪法落地**——用户拍板往平台靠（第三方优先 + 运行时动态生成插件 + 只按施工顺序彻底落地）：**强制层**（cordis 内核/组合引擎/注册表机制/RPC 平台面+权限咽喉/沙箱内核/审计/Workspace 原语/React root+壳）保留特权，**能力实现**（fs/shell/subprocess/session/graph/llm/subagents/agent loop）全部降为 seam，Rust/engine = 默认 provider。落四件：内核线改强制层/能力契约层二分、agent-plugin D9 特权区清单修订、宪法增补第五条「平台边界」、守卫测试钉住强制层外命令零增长。全文见 [`../archive/agent-platformization-plan.md`](../archive/agent-platformization-plan.md) | 平台化 / 总控 |
| 2026-08-25 | **画布支 Stage-3 会话的出生与管理落地（门禁全绿，实机待验）**——侧边栏 = 当前工作区会话管理（DSH 范式：两源合流/状态点/相对时间/行操作/常驻滚动/可折叠/新建），书脊 = 画布空间导航器（左键定位轻动画 / 拖动落位吸附网格+幽灵预览 / hover 小卡合卷；同日用户反馈：书脊右键菜单取消、改名/删除归侧边栏）；生命周期闭环（新建→落位→收起→删除）经 `plugins/canvas-nav-plugin.ts` 以贡献行消费既有 `ctx.space`（零新增核心空间 API）；旧 SpineRack 二合一（单击换卷/另起一卷/hover 合卷小卡/卷目目录）拆净；`canvas-view-store` 成为书脊↔PaperPanel 视口共享真源（手动拖拽/缩放会取消在途定位动画，不抢 pan）。展开：`canvas-space/stage-3.md`；实机待验见 plans/README 真机欠账表 | 画布支 |
| 2026-08-24 | **agent-plugin P4 存量拆解全清（baton15，commit `3dca1925`）**——B①②④⑤⑥/①b/①c/S4-4/A-1/A-2/A-3/C11 全毕：出厂面三类行源（工具行/prompt 段/capability）全量经插件通道贡献，builtin 出厂表三张退役；十五项第一方 capability 定义零改写留在 blueprint.ts（builtinCapabilities 改名 firstPartyCapabilities）经 capabilitySegmentsPlugin 装载。剩余 C12 dsh-compat 唯一合法挂起（等 DSH 外部信号）；新能力加面从此走通道（docs/plugins/README.md §3） | agent-plugin |
| 2026-08-24 | **session-ledger L0-L3 竣工合入（merge `e677c5c8`；四段 2026-08-23 当日连推，四独立 commit）**——案卷总目 `_ledger.json` 立账（发号对账 max(mem,ledger,scan)/自然迁移/多卷工作集恢复 + 句柄惰性水合）；视图对齐（续开查重换卷不克隆/首页真新建/「已摊开」石青小标）；落盘收编（turn-done 携 doneSid 谁跑完存谁 + beforeunload 全卷保存）；清账守护（_active.json 写入退役 + 一致性守护常驻）。26 判据用例钉死；真机三项欠账见 plans/README 真机验证欠账表 | session-ledger |
| 2026-08-23 | **session-ledger 立项：会话管理收敛（用户定调）**——用户实机反馈「新开一卷没什么用」链路排查出五道裂缝（首页新建空壳/续开不查重/后台卷落盘窗口期/摊开集不持久/发号器可撞号），用户定调「会话管理要做收敛，别堆成屎山」。方案：立案卷总目 `_ledger.json`（档案号=卷号/开合有册/发号对账/落盘收编两动词），书脊列与首页退化为账本两视图；四段 L0-L3 排程，五片载荷仓（msg/agent/exec/草稿/纸面）明确不合并 → [`../archive/session-ledger-plan.md`](../archive/session-ledger-plan.md) | session-ledger |
| 2026-08-23 | **插件化战略换轨：D9 自研为主，不等 DSH（用户拍板）**——用户定调「所有能拆的全部插件化，特权区尽可能小；最终方便自己开发，第三方开放是顺手的事」。触发条件从「DSH 信号」改为「用户想要」，现已触发。特权区清单定案（cordis 内核/五 service 壳/RPC 边界/agent 流式核心/Workspace 原语/Rust 壳）只减不增；第一方插件仍编译期打包（收益=解耦/可禁用/多窗口并行，非物理分包）；Rust 侧插件化标准形态 = 外部 MCP server。DSH 信号降级为「将来要不要写 compat 装载层」；形状兼容零依赖原则保留。P4 重写为三股交替：通道补齐 + 存量拆解①-⑥ + P4a 调研 | agent-plugin / 总控 |
| 2026-08-23 | **agent-plugin P2+P3 竣工（commit d772af37 + 15930f65）**——执行原语 code_execution 全段落地：R5 spike 验证 webview Worker 无 CSP 阻碍（blob module worker 全绿）→ 用户拍板方案 A（纸壳新增「程文」code 块）→ C4-C8 全判据（协议腰线：correlation-id 一次性 + 敌意校验 + 无损 JSON + 双簿预算；嵌套审计落 session-log；读并行写串行；门禁不豁免）；P3 cordis 收口（CodeRuntimeService → ctx.codeRuntime，DSH 接缝纪律：runtime 不知道工具和会话；convergence 零漂移）。baseline 变更 baseline-change-request-code-execution.md 随 commit 生效 | agent-plugin |
| 2026-08-22（深夜） | **V5 拆除施工毕（commit a621b1e6，-16843 行，四件套门禁全绿）**——旧观测台前端整体摘除，纸壳成为唯一主界面。拆除：App 树 chrome 全族 + ChatBeacon 聊天视图族 + 图谱 dock 面板族 + 壳行 12→9（graph/nav/dataflow-parser）+ workspace 渲染面（**数据面保留**——graphData 分页装载/graph-updated/runCheck 简报注入照旧服务 Agent 工具）+ shell-store.view + 星图画布/氛围层（three.js 出 bundle）+ paper preset 行。承接：PromptShelfHost（权限/ask 卡独立浮层——会话刚需）、WinControls（窗口控制迁书眉/首页顶栏）、「关卷」回案卷首页（SessionsHome 常驻基底层）。遗留：scene/ui 休眠层 sweep、纸壳交互欠账（C8+）见 r5-polish-backlog | paper-shell |
| 2026-08-22（深夜） | **V5 提前拍板：摘除旧观测台前端**——用户原话「这个狗日的旧前端就不能摘了吗，就非得挂在这」（taste-ledger 同日落账）。**显式翻案**：推翻「纸能住人 + R3 访谈后再 V5」的共居期排序；**R3 退役访谈作废**（拆除不等判据——退役判据无对象）。纸壳（注疏案卷工作台）成为唯一主界面。当日背景：兰台注疏定稿全量落地（7 commit 门禁全绿）+「纸面板即聊天」成立（composer 接 core.sendMessage，消息流经 translate.ts 转译为块）——拆除的可用性前提与审美前提同日齐备 | paper-shell / 总控 |
| 2026-08-22 | **兰台注疏设计定稿全量落地（handoff 当日施工）**——用户侧 Open Design 交付定稿包（原型 + `docs/design/lantai-design-spec.md` + token 表）。产品化：tokens.css 换矿物墨色 + `--obs-*` 过渡别名层（1338 处引用零断链）；fonts.ts 四体换代；PaperPanel 七类块注疏化转录；案卷首页换皮（兰台印章 + 界栏列表）；全局术语替换（会话→案卷 / 发送→拟文）；favicon 亭台图标。门禁全绿 | paper-shell / 总控 |
| 2026-08-22 | **产品更名执行：兰台 / Lantai 全量落地**（同日拍板→施工，零外部用户 = 标识符窗口开放）——应用身份：productName/窗口标题「全息观测站」→「兰台」；identifier `com.hologram.hg` → `com.lantai.app`（本机 API Key 需重录一次）；updater endpoint 指 `834063245-creator/Lantai`（**repo 改名由用户在 GitHub 执行，改名前不打发布包**）。**HoloGram 降级为图谱引擎专名**（工具域 `hologram(...)` / `.hologram/` / `HOLOGRAM_*` / crate 名不改）；应用层内部标识全换（`__lantai_plugin_host__` / `__lantai_*_stores__` / crate `lantai`）。system prompt persona 随之改 → baseline 重录（两轨，change request 在案） | 总控 |
| 2026-08-22 | ~~产品命名拍板：兰台 / Lantai~~（当日稍晚升级为上条「全量落地」，冻结预案作废） | 总控 |
| 2026-08-22 | **V1-R3 全弃 → 止损 → 参照物板收敛：印刷品方向通过（首个正判定）**——三假说证伪后经参照物板（R1 iA × R5 Craft）定方向；材质暂定 R4 零纹理；风格源 direction-b 排印宪章 v3 转录为 r5 档；宪章 token 表 / 字形盘点 / 表面清单三件备料落地；打磨 A 段（转录缺口）清账，B 段队列待 vision 会话；用户疲劳态落账，人判项挂起 | paper-shell |
| 2026-08-22 | **workspace-flip 竣工**（批 1-5 五 commit：会话首页 / 零目录会话 / 打开流两段化 / bootShell 接线 / 纸壳 preset——S3 解锁）→ 已归档 `docs/archive/workspace-flip/` | workspace-flip |
| 2026-08-22 | workspace-flip 设计件 D9 出稿（六批序列）+ W1 访谈四项粒度拍板 | workspace-flip |
| 2026-08-22 | 纸壳 V3b 壳装配竣工（纸面板迁组合层贡献 + 块渲染器第五通道 ctx.renderers） | paper-shell |
| 2026-08-22 | 纸壳 V3a 骨架内核竣工（pretext 上游测量 / 虚拟化 / 抽纸条 / 方位感 / IME 谓词） | paper-shell |
| 2026-08-22 | 顺序拍板：先结构（#9）后视觉——视觉打磨排后，避免「什么都没做完」陷阱 | 总控 |
| 2026-08-21 | 走查弹毕业（用户判定「感觉是对的」）；块粒度定案（#10 抽纸条） | paper-shell |
| 2026-08-20 | 组合层 S4 竣工（preset realm / 热重载 / npm 安装 / hello 闭环） | composition |
| 2026-08-20 | 组合层 S0-S2 竣工；paper-shell 立项（R1/R2 访谈）；三阶段串行排程拍板 | composition / paper-shell |
| 2026-08-19 | 前端总线归零 + ui/ 拆分竣工（事件系统退役，zustand 统一）→ 已归档 `docs/archive/eventbus-zero-and-ui-split-plan.md` | 前端 |
| 2026-08-19 | ui/react 岛层退休 → 已归档 `docs/archive/ui-react-island-retirement-plan.md` | 前端 |
