# Copyright (c) 2026 Wenbing Jing. MIT License.
# SPDX-License-Identifier: MIT

[package]
name = "hologram"
version = "10.4.0"
description = "全息观测站"
authors = ["Hologram Team"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [], default-features = false }

[dependencies]
tauri = { version = "2", features = ["wry"] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
walkdir = "2"
ureq = "3"
regex = "1"
encoding_rs = "0.8"
glob = "0.3"
url = "2"
portable-pty = "0.9"
chrono = "0.4"
tokio = { version = "1", features = ["sync", "time", "net", "io-util", "rt", "rt-multi-thread", "macros", "io-std"] }
tokio-tungstenite = "0.24"
futures-util = "0.3"
reqwest = { version = "0.13", default-features = false, features = ["stream", "rustls", "http2"] }
hyper = { version = "1", features = ["server", "http1"] }
hyper-util = { version = "0.1", features = ["server", "tokio", "http1"] }
http-body-util = "0.1"
http-body = "1"
libloading = "0.9"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
tracing-appender = "0.2"
base64 = "0.22"
hologram-engine = { path = "../engine" }
tauri-plugin-updater = "2.10.1"
tauri-plugin-window-state = "2"

[target.'cfg(windows)'.dependencies]
# UIA 进程内 COM 客户端（desktop_uia_*）+ 物理输入合成（SendInput/剪贴板）
windows = { version = "0.62", features = [
    "Win32_UI_Accessibility",
    "Win32_UI_WindowsAndMessaging",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_System_Com",
    "Win32_System_Ole",
    "Win32_System_Memory",
    "Win32_System_DataExchange",
    "Win32_Foundation",
] }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
